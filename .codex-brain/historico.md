# Historico

## 2026-06-01

- FEATURE: 2 gerentes por agente recebem o relatorio automatico de transferencia (build pedro-webhook-v2 `2026-06-01-two-managers-v21`). Antes: 1 gerente em `wa_ai_agents.gerente_phone`. Agora: + coluna `gerente_phone_2` (via Management API; migration 20260601190000). UI (`AgentCrmEquipeTab`, aba Vendedores): botao "+" adiciona 2o gerente (max 2), com remover; salva os dois; tsc OK. Helper unico `_shared/transfer/managers.ts` `managerPhones(agent)` -> [gerente_phone, gerente_phone_2] (digitos, dedup, >=10). Todos os senders passam a mandar aos 2: `manual-transfer` (loop), `bulk-transfer-leads` (map vira string[]). DESCOBERTA IMPORTANTE: o relatorio ao gerente NAO era enviado nas transferencias AUTOMATICAS do v2 (orquestrador handoff + cron handleV2Followup so notificavam o VENDEDOR; gerente so no manual-transfer/v1/bulk). Como todos estao no v2, isso significava que o gerente nao recebia relatorio nas transferencias automaticas. CORRIGIDO: adicionado o relatorio ao(s) gerente(s) no orquestrador (apos seller notif, via sendPedroText) e no cron handleV2Followup T3 (via sendUazapiTextMessage; query do agente += gerente_phone/gerente_phone_2). Deploy: pedro-webhook-v2, cron-lead-followup, manual-transfer, bulk-transfer-leads. NAO atualizei `uazapi-webhook` (v1, 1 gerente) — esta sendo desativado (Fase B); v1 raramente roda (todos no v2). Nao toca Pedro v1 em runtime.
- UI: removida a aba "n8n" do `AgentFormDialog` (estava so de enfeite, nao usada) — botao da aba + conteudo + import do icone Webhook. NAO removi a logica `syncToN8n` (fica inerte: sem a aba ninguem seta `n8n_webhook_url`, e o sync ja faz early-return quando vazio; campos n8n_webhook_url no banco/payload preservados). tsc --noEmit OK. So frontend.
- FEATURE: aba "Regras" no portal — gerente configura follow-up + transferencia por agente (build pedro-webhook-v2 `2026-06-01-automation-rules-v20`). Nova coluna `wa_ai_agents.automation_rules` (JSONB, nullable; aplicada via Management API pois o MCP DDL estava read-only; migration registrada em 20260601180000). NULL = comportamento LEGADO preservado (follow-up 5/8/12, 3o transfere, resposta 10min, janela fixa). Modelo: `{followup:{enabled,t1_min,t2_min,t3_min,t3_transfers}, transfer:{enabled,seller_response_min,window:{enabled,start,end}|null}}`. Helper UNICO `_shared/automation/rules.ts` (`resolveAutomationRules` com defaults + clamps t1<t2<t3; `isWithinConfiguredWindow`). DESCOBERTA da arquitetura (mapeada): o follow-up 5/8/12 e o `handleV2Followup` (em cron-lead-followup); a escalacao de transferencia roda em 2 motores — `cron-lead-followup` SECAO 1 (10min via created_at, dispara primeiro, NAO gated v1/v2, com defesas) e `transfer-timeout-checker` (15min via confirmation_timeout_at). `pedro-trigger-followup` (followups agendados) e `process-followup-queue` (fila) sao OUTROS sistemas — nao tocados. Por isso o tempo de resposta EFETIVO era 10min -> default da regra = 10. WIRING: (1) `handleV2Followup`: gate followup.enabled, tempos t1/t2/t3, no T3 so transfere se `t3_transfers && transfer.enabled` (senao so despedida, com guard stage<3), timeout = seller_response_min; rotulos "12min" agora dinamicos. (2) `cron-lead-followup` SECAO 1: por-transfer le as regras do agente (join += automation_rules), pula se transfer desligado, usa seller_response_min como tempo (piso da query baixado p/ 1min), janela configurada narrowa dentro do horario operacional global. (3) `transfer-timeout-checker`: idem (skip se transfer off, janela configurada). (4) orquestrador: handoff de qualificacao so executa se transfer.enabled, passa seller_response_min; (5) `executePedroV2Handoff`: timeout configuravel. (6) FRONTEND `AgentFormDialog`: aba "Regras" (toggles + tempos + janela), load/reset/buildPayload, validacao. Decisoes do usuario: transfer OFF desliga TODAS as automaticas (qualificacao+followup+rodizio), manual do portal segue; 3o sem transferir = despede e para; janela incluida. tsc --noEmit OK. Backend deployado (inerte enquanto NULL). LIMITACAO conhecida: janela configurada so NARROWA dentro do horario operacional legado (o portao global nao foi removido p/ seguranca). Nao toca Pedro v1.
- FIX transferencia: vendedor da "OK" mas lead passa pro proximo (build `2026-06-01-seller-ack-claim-v19`). Pos-migracao, usuario reportou: vendedor responde "OK" pra assumir o lead, mas o lead e repassado pro proximo da fila. DIAGNOSTICO (codigo + dado real ai_lead_transfers/pedro_v2_turn_logs): vendedores SAO detectados (intent=seller_ack) e `confirmSellerAck` confirma o transfer + poe lead em_atendimento. MAS: (1) o `transfer-timeout-checker` (escalacao compartilhada) NUNCA checava se o lead ja foi reivindicado — entao um transfer IRMAO pendente (duplicata, ou de outro fluxo: handoff + inatividade 12min + manual) que expira faz a escalacao ROUBAR o lead ja aceito e mandar pro proximo vendedor; (2) o v2 (orquestrador) NAO enviava a confirmacao de volta pro vendedor (o v1 mandava "Atendimento Confirmado!"), entao o vendedor achava que o OK nao funcionou. FIXES: (A) `transfer-timeout-checker`: antes de escalar um transfer expirado, checa se o lead esta `em_atendimento` OU ja tem transfer `confirmed` -> se sim, NAO repassa (so marca expired). Vale v1+v2 (uma vez aceito, o lead e do vendedor). (B) `confirmSellerAck`: ao confirmar, expira os transfers IRMAOS pendentes do MESMO lead (pro checker nao repassar). (C) orquestrador (branch seller): envia "Atendimento confirmado!" ao vendedor quando o ack confirma (paridade v1). Vendedor em chat privado segue detectado por ai_team_members+phonesMatch (numeros 5512...). Verificado: dry_run com telefone de vendedor -> identity.kind=seller (nao vira lead), build v19. Deployado: pedro-webhook-v2 + transfer-timeout-checker.
- FIX agente respondendo em GRUPOS / "vendedor tratado como lead" (build `2026-06-01-ignore-groups-v18`). Pos-migracao (Fase A), usuario reportou o v2 respondendo um VENDEDOR como se fosse lead. DIAGNOSTICO POR LOG REAL: todos os turnos recentes vinham de `jid=...@g.us` (GRUPO do WhatsApp), inclusive um "PAREM"; o agente respondia no grupo ("Oi, Luiz!"/"Entendi, Luiz!" — Luiz Paulo e vendedor em `ai_team_members`). Causa: o v2 NAO ignorava grupos. `remoteJidToPhone` de um group-id (18 digitos) nao casa com vendedor em `ai_team_members` -> `identifyPedroContact` retorna kind=lead -> o grupo vira "lead" e o agente responde. O v1 (`uazapi-webhook` linhas 1963/2017) ja barrava `@g.us` e `@broadcast`. FIX: guard no inicio de `processPedroV2Turn` (logo apos o check de remote_jid, ANTES de identidade/debounce/escrita/envio) que retorna `next_action: ignored_group_or_broadcast` para `@g.us|@broadcast|@newsletter|status@broadcast`. Vendedor em chat PRIVADO segue detectado normalmente (phonesMatch + ai_team_members ja funcionavam para numeros 5512...; phonesMatch ate trata o 9o digito). Verificado: dry_run com jid de grupo -> ignorado; 0 leads de grupo em ai_crm_leads (nada a limpar, follow-up nao vai mandar pra grupo). Deployado em prod.
- MIGRACAO Pedro v1 -> v2, FASE A (rollout reversivel, v1 mantido como rede). Objetivo do usuario: substituir o v1 pelo v2 em TODAS as contas, sem reconectar instancias, e depois remover o v1. Estrategia escolhida: FASEADA com v1 como rede de seguranca (reapontar tudo pro v2 agora, validar em producao, remover v1 so na Fase B). DESCOBERTA da arquitetura: a URL do webhook fica no lado do Uazapi (POST {api_url}/webhook com o token da instancia) -> trocar a URL NAO exige reconectar/QR. O roteamento v1/v2 era decidido por `resolveWebhookFunction` em `sync-evolution-webhook` (ja respeitava o flag global) e `create-evolution-instance` (so olhava allowlist). `uazapi-webhook` (v1, 3850 linhas) e o Pedro SDR de concessionaria (BNDV 133x, transfer 233x, feature flags BANT/LEAD_SCORING/OBJECTION_PLAYBOOKS; campaign/agent_type 0x) — NAO e webhook geral, entao migrar pro v2 nao afeta campanhas/outros. Cenario (5 usuarios, 11 instancias): douglasaloan/Aloan (ja v2); brunolira0312/Carvalho (4 instancias conectadas = cliente VIVO); wandercarvalho31/Sarah (1 desconectada); wanocarvalho2/roberto (4, sem agente ativo); flavig/fla (1, sem agente ativo). PASSOS FEITOS: (1) `PEDRO_V2_ENABLED=true` (secret) — gate global liberado p/ todos; MUTATIONS/SEND ja eram true. (2) `create-evolution-instance.resolveWebhookFunction` ganhou checagem do flag global (consistencia c/ sync; instancia nova ja vai pro v2). (3) deploy de pedro-webhook-v2 (build `2026-06-01-v2-rollout-v17`), sync-evolution-webhook, create-evolution-instance, cron-lead-followup (pegar o secret). (4) VERIFICACAO: dry_run no pedro-webhook-v2 c/ instancia do Carvalho -> gate `global_flag`, sem erro. (5) REAPONTAMENTO: script setou o webhook de 10/11 instancias p/ pedro-webhook-v2 (confirmado via GET antes/depois); `wander-carvalho-34` deu 401 (desconectada) e ficou no v1 — reapontar quando reconectar (sem trafego ate la). Follow-up: `cron-lead-followup` e UM cron com branch por usuario (isPedroV2User respeita o flag global, linha 343) -> com flag on, todos caem no branch v2; branch classico v1 fica inativo -> SEM follow-up duplicado. REVERSAO instantanea: `PEDRO_V2_ENABLED=false` + reapontar webhooks de volta p/ uazapi-webhook (v1 segue deployado intacto). PENDENTE FASE B (so apos validar Carvalho em producao): hardcodar pedro-webhook-v2 no roteamento, deletar `uazapi-webhook` + helpers `pedro-*` mortos (apos analise de alcance), e o gate. Nada do v1 foi removido ainda.
- FIX foto do CARRO ERRADO (build `2026-06-01-photo-model-match-v16`). Sintoma (apos v15): lead pediu "Me manda fotos do renegade", o agente ENVIOU foto (v15 ok) mas a IMAGEM era de um Chevrolet (prata) enquanto o TEXTO dizia "Jeep Renegade 1.8 4X2 FLEX 16V AUT 2018". Causa raiz em `pickReferencedVehicle` (orchestrator): a ordem de selecao era (1) ordinal "primeiro/segundo" -> (2) `pickVehicleByMessageAttributes` cujo gate `hasDiscriminator` so aceita COR/CAMBIO/CARROCERIA/ANO (NOME DE MODELO NAO conta) -> (3) memoria -> (4) DEFAULT index 0. Como "renegade" e nome de modelo (sem cor/cambio/ano), nada casava e caia no index 0 = PRIMEIRO carro do pool (o Chevrolet). O texto saia certo porque o LLM de fechamento lia o pedido do lead, mas a foto vinha do veiculo errado. FIX: nova funcao `pickVehicleByModelName(message, vehicles)` que pontua cada veiculo pelos tokens de marca+modelo presentes na mensagem (>=4 chars valem +4, 3 chars +2) e escolhe o de maior score; plugada em `pickReferencedVehicle` DEPOIS de ordinal/atributos e ANTES da memoria/default (nome citado vence o contexto antigo). Validado por node nos cenarios reais: "fotos do renegade" -> Jeep Renegade; "fotos do tracker" -> Chevrolet; "fotos do asx" -> Mitsubishi ASX; pool de 5 Renegades -> um Renegade; "mais fotos"/"fotos do painel" -> cai na continuidade (inalterado); "fotos do segundo" -> ordinal (inalterado); "fotos do prata" -> atributo (inalterado). Nao toca Pedro v1. Deployado em prod.
- FIX "prometeu foto e nao enviou" (build `2026-06-01-photo-from-stock-v15`). Sintoma: lead pediu "Me manda fotos do renegade", o agente respondeu "vou separar as fotos certas do Jeep Renegade" e NAO enviou nada (o follow-up de 5min so repetiu a promessa). DIAGNOSTICO POR LOG REAL (`pedro_v2_turn_logs`): intent classificado `photo_request` (certo), mas `next_action: stock_search_required`, `reply_source: brain_stock_reply`, `photo_target: null`, `media_count: 0`, `reason: enforced_current_vehicle:alias:renegade`. Ou seja: o planner rebaixou para `stock_search` (o orquestrador SO envia foto quando `brainPlan.action==='photo_request'`, linha ~1081), entao mesmo achando 5 Renegades NA MESMA busca, respondeu so TEXTO e o cerebro "inventou" que ia separar as fotos. Causa do rebaixamento: `normalizePlan` (planner, linha ~356) usa `photo && hasPresentedVehicles && !possible_new_topic` para escolher photo_request — e `hasPresentedVehicles` era FALSE. ACHADO MAIS PROFUNDO (anotado em pendencias): `pedro_conversation_state.veiculos_apresentados` esta SEMPRE vazio (15 states checados, todos 0) e a lead do numero de teste (558588323679) nao existe em `ai_crm_leads` nem por sufixo -> resolucao/persistencia de lead com anomalia (NAO corrigido agora, exige investigacao dedicada). FIX robusto que NAO depende disso: no orquestrador, se o lead pediu fotos EXPLICITAMENTE (`messageAsksForPhotos(text)`) E ha veiculos para mostrar — da memoria OU de uma busca ESPECIFICA (nao generica) recem-feita no MESMO turno (`stockResult.items`) — monta `buildVehiclePhotoReply` com esse pool e ENVIA as imagens, mesmo com `action==='stock_search'`. Tambem passa a salvar os veiculos da busca fresca em `veiculos_apresentados` quando as fotos saem dela (para "mais fotos" funcionar). Guard: so dispara com palavra de foto explicita (preserva o guard anti-foto-nao-pedida do "Gostei"). Estoque tem `fotos[]`+`principal_image` (stockSearch linhas 475-476), compativel com `selectVehiclePhotos`. Nao toca Pedro v1. Deployado em prod.
- FIX envio silenciosamente perdido / "digitando e nada" (build `2026-06-01-send-retry-v14`). Sintoma: usuario mandou "Bom dia" e o agente ficou em "digitando" sem responder. DIAGNOSTICO POR LOG REAL (`pedro_v2_turn_logs`): o cerebro GEROU a resposta normalmente (`reply_source: brain_reply`); a falha foi SO no envio — `send_result.ok=false`, erro `message-sendText: HTTP 404 host not mapped`. Os 3 endpoints do `sendPedroTextOnce` falharam com o mesmo "host not mapped" (so o ultimo erro fica no log). Causa raiz: queda TRANSITORIA da sessao no Uazapi (servidor nao tinha a instancia mapeada naquele instante; o "online" do WhatsApp nao garante a ponte Uazapi). Confirmado ao vivo: `GET /instance/status` da instancia `agente-ia-f76j` voltou `connected:true, loggedIn:true` minutos depois — ou seja, foi um blip, NAO o cerebro nem a v13 (name-strip). PROBLEMA REAL exposto: sem retry, um blip desses PERDE a mensagem em silencio. FIX (so em `uazapiSender_20260524.ts`, `sendPedroTextOnce`): retry com backoff curto — ate 3 rodadas (1 + 2 retries) dos 3 endpoints, backoff 1.2s/2.0s; `try/catch` por tentativa (erro de rede nao aborta mais o envio). SO re-tenta quando `res.ok=false` (envio NAO confirmado) -> nao duplica mensagem; caminho de sucesso identico (apenas ganhou o campo `round`). Nao toca cerebro/planner/BNDV/midia nem Pedro v1. Deployado em prod (`seyljsqmhlopkcauhlor`).

## 2026-05-29

- FIX nome repetido (DETERMINISTICO, build `2026-05-30-name-strip-v13`). A instrucao no prompt (v12) nao bastou — gpt-4o insistia ("Oi Douglas!", "Fico feliz, Douglas!", "Sim, Douglas, financiamos!"). Agora, no codigo: `stripLeadNameVocatives` remove o primeiro nome usado como VOCATIVO (", Nome!", "Nome, ...", " Nome!") e recapitaliza o inicio da frase; aplicado no pedroBrainReply SO quando o agente JA usou o nome nas ultimas ~3 msgs (`agentUsedNameRecently`) — entao o nome aparece no maximo de vez em quando, nunca em sequencia. Validado por node nas frases reais dos prints. Tambem: cron-lead-followup (generateFollowupText) deixou de encorajar o nome ("use se soar natural" -> "NAO comece com o nome / nao repita"). Deploy de pedro-webhook-v2 E cron-lead-followup.
- FIX nome repetido demais (build `2026-05-30-name-moderation-v12`). O agente repetia o nome do lead em quase toda msg ("Otima escolha, Douglas!"/"Entendi, Douglas!") mesmo com regra no portal — tendencia do gpt-4o + BLOCO 2 do prompt com instrucoes meio conflitantes ("Sempre trate pelo nome" + "nao repita toda hora"). Fix: regra FORTE no codigo do pedroBrainReply (REGRAS DE CONDUCAO) — usar o primeiro nome com muita moderacao, nunca no inicio da frase, nunca em msgs seguidas; "tratar pelo nome != repetir toda hora" (vale mesmo que o portal mande tratar pelo nome). Reforco no codigo porque a regra do portal sozinha nao bastava.
- PONTO 3 — DEBOUNCE/agrupamento de mensagens (build `2026-05-30-debounce-v11`). Lead mandava dados em varias bolhas (nome/telefone/data) e o agente respondia cada uma. Agora, no orquestrador (apos salvar a msg do usuario, antes de gerar resposta), em conversa real (nao dry_run): espera 7s (`PEDRO_V2_DEBOUNCE_MS`); se chegou msg mais nova do lead (compara id da msg em wa_chat_history), ESTA invocacao retorna `debounced_superseded` (silenciosa); a invocacao da ULTIMA msg junta todas as nao-respondidas desde a ultima resposta do agente (`gatherUnansweredUserText`) e responde como UM turno. Gated v2 (orquestrador v2-only); mensagem unica = mesmo comportamento + 7s. "Ultimo vence" por id evita resposta dupla/perda. Validado por sim node (rajada/unica/com-resposta-no-meio). Limitacao conhecida: invocacoes superseded ainda rodam mediaContext/historico (desperdicio) antes do debounce — otimizacao futura.
- NUNCA ENCERRAR + TRANSFERENCIA SILENCIOSA (build `2026-05-30-never-end-silent-transfer-v10`). Sintoma grave: lead em qualificacao (nome+Azera de troca, sem entrada, nao conhece a loja) respondeu "Nao nao" a "gostaria de saber mais sobre o processo?" e o agente ENCERROU a conversa (seguindo o BLOCO 6 do prompt "encerrar desqualificado" de forma literal). O usuario quer: NUNCA encerrar; lead qualificado/o suficiente -> transfere AVISANDO; lead desqualificado -> transfere SILENCIOSO (sem avisar o lead) para follow-up do vendedor; so desqualifica se o lead recusar EXPLICITAMENTE (um "nao" a uma pergunta do funil NAO desqualifica); insistencia inteligente. Implementado: (a) pedroBrainReply ganhou instrucoes de INSISTENCIA (nunca encerrar/despedir como fim; "nao" a pergunta do funil nao desqualifica), regra de transferir apos conduzir o funil mesmo com respostas "nao" (pronto_para_transferir mais brando), e novo campo `transferir_silencioso` (true SO em recusa explicita). (b) orquestrador: novo caminho `silentTransfer` reusa executePedroV2Handoff (transfere + notifica vendedor) mas a msg ao lead e so a despedida gentil do cerebro (sem anunciar); notificacao do vendedor diferenciada ("LEAD PARA FOLLOW-UP" vs "QUALIFICADO"). Isso supera o "encerrar" do BLOCO 6. Gated v2.
- FIX (build `2026-05-30-audio-photo-guard-v9`) — 2 regressoes pegas nos logs:
  - FOTO no "Sim" durante qualificacao: o `acceptedPhotoOffer` disparava porque `hasRecentPhotoOffer` olhava as ULTIMAS 5 msgs do agente e dava match em qualquer mencao a foto — entao depois de mandar fotos e ir pra qualificacao ("tem carro na troca?"), um "Sim" virava "sim, manda fotos" (log: reason `enforced_accepted_recent_photo_offer`). Fix: `hasRecentPhotoOffer` agora olha SO a ultima msg do agente e retorna false se ela for pergunta de qualificacao/agendamento (troca/entrada/pagamento/nome/visita...). O guard anti-foto (v7) ja bloqueia o resto.
  - AUDIO nao entendido: confirmado nos logs que o turno do audio veio `kind="image"`, `text=null` (sem transcricao) por causa do `embeddedImage` (thumbnail do anuncio CTWA) que forcava image. Causa raiz: o `pickKind` (mediaContext) checava so `mime`/blob e NAO o `messageType`/`mediaType` da propria msg — e a Uazapi manda audio como `messageType:"AudioMessage"` / `mediaType:"ptt"|"audio"` (igual o Pedro v1 detecta). Fix: `pickKind` passou a checar msgType/mediaType (audio primeiro). Como `effectiveKind = kind || embeddedImage`, com kind="audio" o audio vai pro Whisper. (O `embeddedImage` ainda faz textos virarem "image"/visao — desperdicio, anotado como otimizacao futura, nao e bug do usuario.)
- AJUSTES pre-lancamento, pontos 1/2/4 (build `2026-05-30-photo5-audio-briefing-v8`). (Ponto 3 = debounce, adiado — ver pendencias.)
  - P1 FOTOS: lote de "mais fotos" saia com 3 quando sobravam poucas fotos. Fix em `selectVehiclePhotos` (orchestrator): preenche SEMPRE ate maxPhotos (5 no overview), priorizando as nao-enviadas e completando com as ja vistas; 1o lote mantem a ordem antiga [0,3,6,7,8]. Validado por sim.
  - P2 AUDIO: a thumbnail do anuncio (Facebook CTWA) vinha em TODA msg e o `pickKind` (mediaContext) checava imagem (via `jpegthumbnail`) ANTES de audio -> todo turno virava kind="image" e o audio ia pra VISAO em vez do Whisper. Fix: `pickKind` checa audio/video/documento ANTES de imagem e nao classifica mais por `jpegThumbnail` (isso e contexto de anuncio, tratado pelo adContext).
  - P4 BRIEFING: campo "Interesse" vinha com o carro de TROCA (ex: Elantra) em vez do desejado (Pajero TR4). Fix: brain reply separa `interesse` (carro que QUER) de `carro_troca` (carro de troca) no `qualificacao_coletada`; briefing mostra "Veiculo de troca:" separado.
- FIX foto "do nada" (build `2026-05-30-no-unrequested-photo-v7`). Sintoma: lead disse "Gostei dele" e o agente mandou 5 fotos sem ser pedido. Causa (confirmada em pedro_v2_turn_logs): o LLM planner devolveu `action="photo_request"` porque fotos ja tinham sido pedidas antes na conversa, mesmo o lead NAO pedindo agora (inconsistente: em outro "Gostei dele" sem foto recente, ele fez reply_only). Fix: guard deterministico em `normalizePlan` (pedroBrainPlanner) — se `action==='photo_request'` mas a mensagem NAO e pedido de foto (`isPhotoText`) NEM aceite de oferta recente (`acceptedPhotoOffer`), rebaixa para `reply_only` (nao envia imagem) e orienta a continuar a qualificacao. Pedidos legitimos ("tem mais fotos?", "fotos do 2", "sim" apos oferta) seguem funcionando.
- SYSTEM PROMPT COMO DRIVER PRINCIPAL (build `2026-05-29-prompt-primary-v6`). Diagnostico: o `wa_ai_agents.system_prompt` do agente "Aloan" ESTA carregado e completo (6415 chars, BLOCO 4-9). O que impedia o prompt de ser seguido NAO era carregamento, e sim o CODIGO: (1) o planner curto-circuitava agendamento -> handoff imediato (transferia sem qualificar); (2) o brain reply dizia "Voce NAO e um fluxo fixo de conversas" (oposto do BLOCO 4-9) + "GANCHOS" genericos que competiam com o passo-a-passo. CORRECAO: (a) pedroBrainReply: removida a frase de "nao e fluxo fixo"; abertura agora diz que o System Prompt do Portal e a DIRETRIZ PRINCIPAL (seguir passo-a-passo/perguntas/transferencia a risca); GANCHOS rebaixados a "apoio" (Portal prevalece); instrucao de coletar o DIA do agendamento + campo `dia_agendamento` em qualificacao_coletada. (b) planner: handoff imediato SO para pedido EXPLICITO de humano; agendamento e "quero comprar" viram reply_only (qualificar antes); a transferencia desses sai pelo `pronto_para_transferir` do brain. (c) orquestrador: guard de transferencia relaxado (nome + algum contexto: interesse/dia/troca/entrada) e passa `qualificacao` ao handoff. (d) transferRouter.executePedroV2Handoff: anexa bloco "DADOS COLETADOS PELO AGENTE" (nome, interesse, dia, troca, entrada, pagamento, conhece-loja) ao briefing do vendedor. Tudo gated v2. Objetivo: por em producao.
  - VARIEDADE DE FOTOS: ao pedir "mais fotos" o agente repetia as mesmas (selectVehiclePhotos era deterministico e o state nao guardava o que ja foi enviado). FIX (orchestrator_20260525_photo_flow.ts): `selectVehiclePhotos` agora recebe `alreadySent[]`, monta a ordem completa (estrategia do alvo + demais indices), exclui os ja enviados e manda o PROXIMO lote (1o pedido = igual a antes; ao esgotar, recomeca). Rastreado em `state.ultima_foto.fotos_enviadas` (acumula por veiculo, reseta se troca de veiculo). Validado por simulacao node.
  - QUALIFICACAO (erro grave): o agente transferia no "vou querer" SEM seguir o passo-a-passo do system prompt (BLOCO 4: nome/troca/entrada/loja). Causas: planner (Etapa C) dava handoff cedo demais; brain reply nao recebia `negociacao` nem instrucao de seguir a sequencia. FIX: (1) planner so faz handoff imediato em AGENDAMENTO ou pedido EXPLICITO de humano; "quero comprar/vou querer" vira `reply_only` (qualificar primeiro). (2) brain reply (pedroBrainReply) recebe `negociacao` no memory_summary + instrucao de QUALIFICACAO OBRIGATORIA (uma pergunta por vez, pulando o ja respondido) e passou a retornar no JSON `qualificacao_coletada` e `pronto_para_transferir`. (3) orquestrador transfere quando o planner manda (agendamento/humano) OU quando o cerebro marca `pronto_para_transferir` COM qualificacao minima (nome + troca/entrada/forma de pagamento). Dados coletados serao a base do feedback do gerente (proximo passo). Persistencia de `negociacao` no state ficou para o passo do feedback (hoje o cerebro re-deriva do historico recente). Tudo gated v2.
  - Lead apagado no CRM voltava a ser tratado como conversa em andamento (sem reapresentacao). CAUSA: `ensurePedroV2Lead`/`findPedroV2Lead` (leadMemory.ts) nao retornavam `created_at`, entao o orquestrador (linha ~842) passava `undefined` e NAO filtrava o historico por data -> carregava `wa_inbox`/`wa_chat_history` antigos por remote_jid. FIX: incluido `created_at` nos 2 selects -> lead recriado tem created_at=agora -> historico filtrado ignora msgs antigas -> recomeca do zero. Tambem: `PedroSDR.tsx` (delete do lead) passou a apagar `pedro_conversation_state` junto (faxina da memoria).
  - Etapa C: planner (`pedroBrainPlanner`) ganhou regra de HANDOFF (agendou visita / pronto pra comprar / pediu humano; conservador) + re-afirmacao no `normalizePlan` para nao ser sobrescrita pelas regras de veiculo (foto explicita ainda vence). Orquestrador: quando `needs_handoff`, executa `executePedroV2Handoff` (novo em transferRouter.ts) que reusa `chooseSellerForPedroTransfer` + briefing gpt-4o-mini + cria `ai_lead_transfers` + marca `status='transferido'` (NAO mexe em status_crm) + notifica o vendedor; o agente manda a despedida avisando que um consultor vai chamar. Como o lead sai de novo/interessado, o cron de follow-up PARA SOZINHO. Guard atomico evita transferencia dupla. Tudo gated v2 (orquestrador v2-only) -> bug fica contido a conta do usuario.
- Sessao de analise do Pedro v2 + inicio da "Fase 0" (higiene de segredos), sem alterar runtime: Pedro v1 e Pedro v2 seguem intactos (nenhuma Edge Function/deploy tocado).
- Mapeado o caminho de codigo VIVO do Pedro v2 a partir de `pedro-webhook-v2/index.ts`:
  - orquestrador ativo: `orchestrator_20260525_photo_flow.ts`;
  - modulos ativos: `server.ts`, `leadMemory.ts`, `contactIdentity.ts`, `intentRouter_20260525_sales.ts`, `transferRouter.ts`, `pedroBrainPlanner_20260525.ts`, `pedroBrainReply_20260525.ts`, `stockSearch_20260525_photo_flow.ts`, `vehicleResolver_20260525_brain.ts`, `adContext_20260525.ts`, `mediaContext_20260524.ts`, `uazapiSender_20260524.ts`, `phone.ts`, `types.ts`;
  - "cerebro" do agente usa OpenAI `gpt-4o` (planner temp 0.1 / reply temp 0.35), nao Claude;
  - identificados ~18 arquivos versionados possivelmente mortos em `_shared/pedro-v2`. NAO removidos: 9 functions `pedro-*` (identify-contact, intent-router, lead-memory, transfer-router, stock-search, sales-reply, crm-sync, seller-ack, message-sender) + `sync-evolution-webhook` tambem importam de `_shared/pedro-v2`. Limpeza so apos analise de alcance completa, para nao arriscar Pedro v1.
- Higiene de segredos (apenas arquivos locais/doc, sem runtime):
  - removidos os valores reais de tokens de `pedro_v2_architecture_and_credentials.md` (access token Supabase staging+prod e GitHub PAT), trocados por placeholders + aviso; arquivo adicionado ao `.gitignore`.
  - `.gitignore` passou a ignorar `/.env`, `/secrets.txt` e `pedro_v2_architecture_and_credentials.md`.
  - CONFIRMADO que `.env` e `secrets.txt` estao RASTREADOS no Git (ja no repo remoto): `.env` contem `SUPABASE_SERVICE_ROLE_KEY` e `LEGACY_SUPABASE_SERVICE_ROLE_KEY` (entre outras); `secrets.txt` referencia ANTHROPIC, OPENAI, EVOLUTION/Uazapi, META e SERVICE_ROLE. Pendente: `git rm --cached` + ROTACAO de todos esses segredos.
- Identidade do dev confirmada: usuario e `dev-aloan` (git `user.name=dev-aloan`, email `douglasaloan@gmail.com`); `dev-wander` e o socio (Wander Carvalho, `carvalho@scalpergx.com.br`). Descoberto que a branch `dev-aloan` esta OBSOLETA (~769 commits atras, sem Pedro v2/brain); o trabalho real do Pedro v2 e dos dois esta na branch compartilhada `dev-wander` -> `main`. Regras validas do usuario estao em `.agents/instructions/user_preferences.md` (trabalhar como `dev-aloan`, levar para `main`); o `CLAUDE.md` do repo foi escrito para o socio.
- GIT FEITO: commit de higiene `f253efe` na `dev-wander` (autor `dev-aloan`) e cherry-pick `e59c341` na `main`, ambos com push para o `origin`. `.env` e `secrets.txt` deixaram de ser rastreados (arquivos mantidos localmente). Commit nao altera codigo de aplicacao (so `.gitignore`, `.codex-brain` e remocao de tracking). FALTA: ROTACIONAR os segredos vazados e, opcionalmente, limpar o historico do Git (filter-repo/BFG, destrutivo, coordenar com o socio).
- IDENTIDADE GIT travada nesta maquina: auditoria confirmou `user.name=dev-aloan`/`user.email=douglasaloan@gmail.com` no `.git/config` local, sem config global, sem env var, sem hooks previos. Modelo de trabalho: dev-aloan commita desta maquina, Wander commita da dele, os dois empurram para a `main`. Para impedir confusao de identidade (ontem o Antigravity deixou escapar um commit como `dev-wander`), foi instalado um hook local `.git/hooks/pre-commit` que BLOQUEIA qualquer commit cujo autor != `douglasaloan@gmail.com` (testado: passa para dev-aloan, bloqueia autor forcado). O hook e local (nao versionado, nao afeta o Wander); se reclonar, recriar. Bypass so com `--no-verify`.
- MELHORIA Pedro v2 (camada de ENVIO, sem tocar inteligencia): quebra de mensagens conversacionais. Confirmado que o system prompt do portal (`wa_ai_agents.system_prompt`, via aba general manual OU aba "Funil do Agente" que gera/sobrescreve com backup) JA e injetado como personalidade no `pedroBrainReply_20260525.ts`. Decisao: personalidade vem do portal; o "BLOCO 2 - comportamento obrigatorio fixo" e as tools/contrato tecnico ficam no codigo (rede de seguranca, cliente pode esquecer de preencher). Ajuste feito so em `uazapiSender_20260524.ts`: split conversacional `maxParts 2->3`, `minLength 260->130`, e `calculatePedroV2DelayMs` de 8.5-18s para 2.5-9s proporcional. NAO toca roteamento estoque-vs-conversa (lista de estoque continua mensagem unica via `typingOnly`), nem cerebro/BNDV/midia, nem o Pedro v1 (`uazapi-webhook` tem copia propria do splitter). Validado por simulacao node nas mensagens reais do print. Deployado em prod (build `2026-05-29-message-split-v1`) e validado pelo usuario no WhatsApp ("quebra esta boa").
- ETAPA A (split por LLM) — refinamento do split: criado `_shared/humanization/llmMessageSplit.ts` (`splitMessageForHumanizationLLM`) que usa `gpt-4o-mini` (temp 0) so para ESCOLHER os cortes, com regra de nunca separar modelo/ano/preco. Guarda anti-alteracao: se a juncao dos pedacos (fingerprint sem espacos) != original, ou >maxParts, ou erro/sem API key -> cai no splitter heuristico. `uazapiSender_20260524.ts` passou a usar `await splitMessageForHumanizationLLM(...)` no caminho humanize (conversa); lista de estoque (typingOnly) intocada. Guarda validada por teste node. Build `2026-05-29-llm-split-v2`. Etapas B (follow-up 5/8/12 contextual) e C (transferencia inteligente/parar follow-up) ficaram para depois, uma de cada vez, gated na conta do usuario.
- INCIDENTE corrigido: durante o vai-e-vem de branches da higiene de segredos, o checkout deletou os arquivos LOCAIS `.env` e `secrets.txt` (uma branch rastreava, a outra nao + gitignore). Restaurados do historico (`git show 24b8207:.env`/`secrets.txt`), seguem ignorados. LICAO: ao fazer `git rm --cached` de arquivo + trocar de branch onde ele ainda e rastreado, o working file pode ser apagado; restaurar do git e seguir.
- REGRESSAO anuncio->estoque diagnosticada e corrigida (build `2026-05-29-ad-stock-fix-v3`). NAO foi causada pelo split (camada de envio); estava dormindo na `main` desde commits pos-`2026-05-27` (provavelmente `24b8207` scoring) e o redeploy de hoje a expos. Diagnostico por `pedro_v2_turn_logs`: link de anuncio dava `next_action=stock_search_required` mas `stock_result_count=0`, enquanto "Quais argos voce tem?" dava 3 — MESMA query "Fiat Argo"/hatch. Causa: `buildSearchText` (stockSearch_20260525_photo_flow.ts) incluia o `filters.ad_context` (blob cru do Facebook: icom, motors, story_fbid, numeros...) no texto de scoring; a penalidade de -10 por termo de modelo nao-casado (~21 termos-ruido = -210) zerava o score do carro certo. FIX: novo `buildScoringText` (so query/marca/modelo/versao, SEM ad_context) usado em `scoreVehicle` e `detectDynamicModelTerms`; `ad_context` continua so para inferir TIPO. Confinado ao v2 (so o orquestrador ativo importa esse arquivo; v1/Marcos nao). Validado por simulacao node (-210 -> 0).

- ETAPA B (follow-up contextual 5/8/12) implementada no `cron-lead-followup/index.ts`, GATED pela allowlist do Pedro v2 (`isPedroV2User` espelha `isPedroV2EnabledForUser`: PEDRO_V2_ENABLED / _ALLOWED_USER_IDS / _ALLOWED_USER_EMAILS via auth admin, cache por user). No loop: `if (isPedroV2User) { handleV2Followup(); continue; }` — caminho classico 5/10 fica BYTE-IDENTICO para v1/Marcos/outras contas. Novo fluxo v2: 5min=mensagem contextual (gpt-4o-mini usando recent_turns+persona do portal), 8min=pergunta se precisa de ajuda, 12min=despedida amigavel + transferencia (REPLICADA do fluxo atual, sem refatorar o bloco antigo). Controle de etapa em `pedro_conversation_state.state.followup` {stage,anchor} (sem migration); `anchor`=last_agent_reply_at reseta o ciclo quando o lead volta a falar. Fallbacks fixos se a LLM falhar. Marcador `build: etapa-b-followup-5-8-12-v1` na resposta do cron. Logica validada por simulacao node (progressao 5/8/12 sem repeticao, reset por anchor, catch-up se o cron pular). PENDENTE Etapa C: parar follow-up + transferir quando o lead qualifica/agenda (hoje, se agendar e seguir 'novo', o follow-up ainda dispara ate a C). Risco de bug no codigo novo fica CONTIDO a conta v2 (leads nao-v2 nem entram em handleV2Followup).

## 2026-05-27

- Pedro v2: Reestruturação da inteligência para torná-la 100% dinâmica e alinhada ao prompt do portal.
- Anúncios: Adicionada extração de marca/modelo por LLM (`gpt-4o-mini`) em anúncios no `adContext_20260525.ts` e remoção de filtros de modelos locais fixos.
- Resolvedor de Veículos: O `vehicleResolver_20260525_brain.ts` passou a suportar detecção dinâmica por marcas conhecidas + termos significativos seguintes (ex: Fiat Cronos, Peugeot 208, etc.).
- Busca de Estoque: O `stockSearch_20260525_photo_flow.ts` foi atualizado para extrair termos de modelo da query (`modelTerms`) de forma dinâmica, aplicando bônus e penalidades sem depender de listas estáticas de keywords ou aliases.
- Personalidade: Desativado o formatador determinístico rígido em `pedroBrainReply_20260525.ts` para que as respostas usem as próprias palavras da IA orientadas pelo System Prompt do Portal. Simplificação e reestruturação do prompt da OpenAI para priorizar a personalidade do portal.

## 2026-05-26

- Pedro v2: reforcada a leitura de anuncio/link com tentativa de localizar e baixar thumbnail/imagem do card antes da analise por visao; isso melhora casos de Facebook/Instagram em que o texto do link nao traz o veiculo, mas a imagem traz.
- Pedro v2: ampliado limite de busca/listagem do estoque BNDV e memoria de veiculos apresentados para evitar cortar modelos disponiveis e quebrar referencias como "o segundo" ou "tem mais modelos".
- Pedro v2: `pedro-webhook-v2` implantada em producao com build `2026-05-26-ad-thumbnail-full-stock-v1`.

## 2026-05-26

- Pedro v2: refatorado o fluxo de resposta final para evitar comportamento de robo/script fixo.
- O orquestrador agora carrega ate 24 turnos recentes combinando `pedro_conversation_state.recent_turns` com historico real de `wa_inbox`.
- `pedroBrainReply` deixou de responder small talk por regra deterministica e passou a montar mensagens reais de chat para o LLM usando o system prompt cadastrado no portal.
- A tool de fotos continua selecionando/enviando midias, mas a mensagem humana de fechamento agora passa pelo cerebro com contexto da tool.
- Deploy realizado da Edge Function `pedro-webhook-v2` no Supabase de producao `seyljsqmhlopkcauhlor`.

## 2026-05-23

- Criado o cerebro local do projeto em `.codex-brain`.
- Auditoria inicial documentou:
  - stack React/Vite/Supabase;
  - rotas principais;
  - deploy Docker/Nginx/Easypanel;
  - Edge Functions principais;
  - separacao Pedro x Marcos;
  - problemas conhecidos e proximos passos.
- Criada e publicada antes deste cerebro a divisao:
  - `/tela-inicial`: hub inicial existente;
  - `/dashboard`: novo dashboard comercial comparativo Pedro x Marcos.
- Build validado antes do push da mudanca de dashboard.
- Criada auditoria tecnica do agente Pedro em `.codex-brain/analise-pedro-agente-2026-05-23.md`, sem alteracao de codigo, apontando falhas de orquestracao, memoria, busca BNDV, contexto de anuncio e plano de reformulacao por fases.
- Protocolado o plano do Pedro v2 em `.codex-brain/plano-pedro-v2.md`: manter Pedro v1 em paralelo, criar orquestrador leve, separar tools/Edge Functions de identificacao, memoria, intencao, estoque, resposta, transferencia, `ok` do vendedor, CRM e envio.

## 2026-05-24

- Iniciada a implementacao paralela do Pedro v2, sem alterar o Pedro v1 (`uazapi-webhook`).
- Criado scaffold de Edge Functions:
  - `pedro-webhook-v2`;
  - `pedro-identify-contact`;
  - `pedro-intent-router`;
  - `pedro-lead-memory`;
  - `pedro-transfer-router`;
  - `pedro-stock-search`;
  - `pedro-sales-reply`;
  - `pedro-crm-sync`;
  - `pedro-seller-ack`;
  - `pedro-message-sender`.
- Criado modulo compartilhado `supabase/functions/_shared/pedro-v2` com identidade por telefone, memoria, roteamento de intencao, transferencia e orquestracao.
- Criada migration `20260524090000_pedro_v2_scaffold.sql` para logs de turno do Pedro v2.
- Pedro v2 foi mantido desligado e protegido por flags de ambiente para evitar escrita/envio acidental.
- Evoluido o Pedro v2 in paralelo:
  - `pedro-stock-search` passou a usar busca real BNDV por tool compartilhada, com normalizacao, tolerancia a erros de digitacao e separacao carro/moto;
  - `pedro-sales-reply` passou a gerar resposta factual com base em memoria, intencao e resultado de estoque;
  - `pedro-message-sender` passou a centralizar envio Uazapi de texto e midia, ainda protegido por `PEDRO_V2_SEND_ENABLED`;
  - `pedro-webhook-v2` passou a orquestrar intencao -> estoque -> resposta -> envio planejado/enviado, sem tocar no Pedro v1.
- Build frontend validado com `npm.cmd run build`; avisos exibidos sao os ja conhecidos do projeto.
- Commit `c2b1294` enviado para `origin/main` com o Pedro v2 paralelo e cerebro local.
- Edge Functions do Pedro v2 publicadas no Supabase de producao:
  - `pedro-webhook-v2`;
  - `pedro-identify-contact`;
  - `pedro-intent-router`;
  - `pedro-lead-memory`;
  - `pedro-transfer-router`;
  - `pedro-stock-search`;
  - `pedro-sales-reply`;
  - `pedro-crm-sync`;
  - `pedro-seller-ack`;
  - `pedro-message-sender`.
- Migration `20260524090000_pedro_v2_scaffold.sql` nao foi aplicada via `db push`, pois o Supabase exigiu `--include-all` para migrations antigas locais que nao constam no historico remoto. Nao usar `--include-all` sem auditoria; o log do v2 falha de forma nao bloqueante se a tabela ainda nao existir.
- Corrigido o caminho seguro para teste controlado do Pedro v2:
  - migration `20260524090000_pedro_v2_scaffold.sql` aplicada manualmente com `supabase db query --linked --file`, sem usar `--include-all`;
  - `pedro_v2_turn_logs` criada em producao;
  - `pedro-webhook-v2` passou a permitir ativacao por `PEDRO_V2_ALLOWED_USER_EMAILS` ou `PEDRO_V2_ALLOWED_USER_IDS`, mantendo o global desligado;
  - `create-evolution-instance` e `sync-evolution-webhook` passaram a apontar usuarios liberados para `pedro-webhook-v2`, mantendo os demais em `uazapi-webhook`;
  - secrets de producao configurados pelos nomes `PEDRO_V2_ENABLED`, `PEDRO_V2_ALLOWED_USER_EMAILS`, `PEDRO_V2_MUTATIONS_ENABLED` e `PEDRO_V2_SEND_ENABLED`, sem registrar valores no repo/cerebro.
- Corrigido primeiro problema real do Pedro v2 no teste por WhatsApp: contexto de anuncio/link.
  - criado resolvedor de contexto de anuncio em `supabase/functions/_shared/pedro-v2/adContext.ts`;
  - `pedro-webhook-v2` passou a ignorar mensagens `fromMe` e a operar em modo real quando mutations estiverem habilitadas, deixando `dry_run` apenas para payload explicito ou flag desligada;
  - orquestrador v2 passou a extrair card/link de anuncio, URL, texto rico e thumbnail/imagem quando disponivel, enriquecendo memoria/intencao antes da busca BNDV;
  - Pedro v2 nao deve responder que "nao consegue acessar links externos"; deve usar metadados do anuncio e, se faltar contexto, perguntar pelo modelo ou print;
  - deploy realizado das Edge Functions `pedro-webhook-v2` e `pedro-sales-reply` em producao.
- Corrigido roteamento real da instancia de teste do Pedro v2:
  - confirmado que chamada direta ao `pedro-webhook-v2` para o usuario `douglasaloan@gmail.com` tratava anuncio/link corretamente;
  - identificado que a instancia Uazapi `agente-ia-hpic` ainda apontava para `uazapi-webhook`, por isso o WhatsApp continuava caindo no Pedro v1;
  - webhook da instancia foi sincronizado para `pedro-webhook-v2`;
  - `sync-evolution-webhook` foi atualizado para respeitar allowlist por user id/email via helper compartilhado do Pedro v2 e nao reverter usuarios liberados para o webhook antigo;
  - function `sync-evolution-webhook` publicada em producao como V8.3.
- Ajustada a seguranca da busca de estoque do Pedro v2 em anuncios:
  - log real mostrou que a Uazapi entregou apenas titulo/descricao genericos do Facebook, sem o modelo do veiculo;
  - o v2 deixou de consultar estoque quando o anuncio nao identifica um veiculo com confianca;
  - adicionada tentativa curta de leitura de metadados/imagem do link antes de desistir;
  - resposta de anuncio sem veiculo agora pede modelo ou print, evitando oferecer carro aleatorio;
  - deploy realizado em `pedro-webhook-v2` and `pedro-sales-reply`.
- Hotfix do Pedro v2 em producao para teste controlado do usuario Douglas:
  - confirmado por chamada remota que `pedro-webhook-v2` esta publicado no Supabase com build `2026-05-24-humanized-ad-reply-v2`;
  - adicionada humanizacao no envio do v2 com presenca de digitacao, atraso realista e divisao de mensagens longas;
  - corrigido fallback de anuncio sem veiculo para se apresentar como Carvalho e pedir modelo/print, sem chutar modelo especifico;
  - reforcada extracao de thumbnails/imagens em payloads Uazapi with arrays de bytes e objetos tipo Buffer;
  - versionados os imports do orquestrador/reply/adContext/sender para evitar cache antigo de modulo compartilhado no Edge Runtime;
  - validado por `dry_run` remoto que o Supabase responde sem oferecer carro aleatorio quando o card do Facebook nao traz o veiculo.
- Hotfix do Pedro v2 para contexto de midia e follow-up:
  - criado `mediaContext_20260524.ts` para extrair contexto de imagens/midias recebidas pela Uazapi, incluindo thumbnails embutidas e tentativa de download via endpoint de midia da Uazapi;
  - criado orquestrador versionado `orchestrator_20260524_media.ts`, mantendo o Pedro v1 intacto;
  - Pedro v2 passou a usar visao para identificar veiculo em print/imagem de anuncio antes da busca BNDV;
  - corrigido registro de `last_agent_reply_at` apos resposta do Pedro v2, requisito para as regras de follow-up de 5 minutos e transferencia por 10 minutos voltarem a enxergar conversas atendidas pelo v2;
  - deploy realizado em producao da Edge Function `pedro-webhook-v2` com build `2026-05-24-media-context-followup-v1`;
  - validado por dry-run remoto com print real de Duster: o v2 identificou `Renault Duster Authentique 1.6 2020 Automático` e acionou busca de estoque. No perfil de teste/instancia `agente-ia-hpic`, a busca retornou 0 unidades desse modelo.

- Hotfix do Pedro v2 para saudacao e deteccao de anuncio:
  - criado `adContext_20260525.ts` para exigir contexto explicito de anuncio/link antes de marcar a conversa como vinda de anuncio;
  - criado `replyGenerator_20260525.ts` com saudacao dinamica pelo horario de Sao Paulo e resposta simples para cumprimentos como "bom dia";
  - o orquestrador `orchestrator_20260524_media.ts` passou a usar os modulos versionados de 2026-05-25;
  - deploy realizado em producao da Edge Function `pedro-webhook-v2` com build `2026-05-25-greeting-ad-context-v1`;
  - validado por dry-run remoto: "Bom dia" respondeu com "Bom dia" e nao entrou como anuncio; texto comum de veiculo fez busca de estoque sem motivo de anuncio; link real do Facebook continuou caindo no fluxo de anuncio sem veiculo seguro.
- Hotfix do Pedro v2 para apresentacao de estoque e fotos:
  - criado orquestrador versionado `orchestrator_20260525_sales.ts`;
  - criado roteador versionado `intentRouter_20260525_sales.ts` com intencao `photo_request`;
  - criado `stockSearch_20260525_sales.ts` preservando ate 12 fotos do BNDV por veiculo em `fotos`;
  - criado `replyGenerator_20260525_sales.ts` para voltar ao formato antigo de lista de estoque, com campos por linha e imagem em markdown;
  - Pedro v2 passou a salvar os veiculos apresentados em `pedro_conversation_state.veiculos_apresentados`;
  - pedidos como "fotos desse segundo" passam a usar o veiculo ja apresentado e enviar ate 5 imagens via Uazapi;
  - saudacao simples ficou menos transacional e mais alinhada ao prompt do consultor;
  - deploy realizado em producao da Edge Function `pedro-webhook-v2` com build `2026-05-25-sales-stock-photos-v1`;
  - validado por dry-run remoto: "Bom dia" responde com saudacao correta e "Voce tem onix?" retorna 5 opcoes do BNDV no formato antigo com imagens.
- Hotfix do Pedro v2 para formatacao de estoque e variedade de fotos:
  - criado orquestrador versionado `orchestrator_20260525_photo_variety.ts`;
  - `pedro-webhook-v2` passou a apontar para o build `2026-05-25-stock-format-photo-variety-v1`;
  - respostas de lista de estoque (`stock_fact_reply`) passaram a preservar quebras de linha, sem o humanizador juntar todos os itens em uma mensagem confusa;
  - pedidos de fotos agora detectam alvo aproximado (`roda`, `painel`, `bancos/interior`, `porta-malas`, `traseira`, `lateral`, `frente`) e selecionam imagens diferentes do BNDV em vez de sempre enviar as primeiras;
  - pedidos gerais de fotos tentam misturar fotos externas e internas usando a ordem das fotos do BNDV;
  - deploy realizado em producao da Edge Function `pedro-webhook-v2`;
  - validado por dry-run remoto na instancia `agente-ia-hpic`: busca por Renegade retornou 5 opcoes, status 200, build correto e quebras em branco entre os itens da lista.
- Hotfix do Pedro v2 para referencia de fotos do mesmo veiculo:
  - criado orquestrador versionado `orchestrator_20260525_photo_flow.ts`;
  - criados `replyGenerator_20260525_photo_flow.ts` e `stockSearch_20260525_photo_flow.ts` para evitar cache antigo no Edge Runtime;
  - `pedro-webhook-v2` passou a apontar para o build `2026-05-25-photo-flow-v2`;
  - pedidos de foto agora gravam `ultima_foto` e `referencia.ultimo_veiculo_*` em `pedro_conversation_state`, evitando que "foto do painel/interior" caia em outro carro depois de "fotos do 4";
  - envio de fotos do Pedro v2 agora manda as imagens primeiro e so depois envia um comentario curto/variado, mais natural para WhatsApp;
  - selecao geral de fotos passou a priorizar ordem aproximada `1, 4, 6, 7, 8` do BNDV; pedidos de painel/interior/bancos priorizam fotos mais internas/laterais tardias antes de voltar ao inicio da lista;
  - nomes de veiculos foram higienizados para reduzir duplicacoes e nomes colados como `RENEGADE1.8`;
  - deploy realizado em producao da Edge Function `pedro-webhook-v2`;
  - validado por dry-run remoto: build `2026-05-25-photo-flow-v2`, busca por Renegade com status 200, 6 itens e quebras de linha preservadas.

## 2026-05-25

- Evolucao do Pedro v2 para orquestrador real, preservando as regras de transferencia que ja estavam funcionando:
  - criado `vehicleResolver_20260525_brain.ts` para resolver o veiculo da mensagem atual, anuncio, midia ou memoria, com prioridade para a mensagem atual;
  - criado `pedroBrainPlanner_20260525.ts` para decidir a proxima acao antes da resposta final (`reply_only`, `stock_search`, `photo_request`, `handoff`, `clarify`);
  - criado `pedroBrainReply_20260525.ts` para gerar a resposta final usando o system prompt do portal, fatos do estoque e horario real de Sao Paulo;
  - `orchestrator_20260525_photo_flow.ts` passou a chamar resolvedor -> planner -> tool de estoque/foto -> resposta final, em vez de seguir um fluxo fixo por regex;
  - corrigido risco de usar memoria antiga quando o lead muda de assunto/modelo, como no caso `oroqui` apos Renegade;
  - cumprimento simples como `bom dia` nao deve mais entrar como anuncio sem payload real de anuncio/midia;
  - deploy realizado em producao da Edge Function `pedro-webhook-v2` com build `2026-05-25-brain-orchestrator-v1`;
  - build frontend validado com `npm.cmd run build`.
- Hotfix do Pedro v2 para memoria curta e perguntas sociais:
  - adicionado `recent_turns` em `pedro_conversation_state.state` para o Pedro v2 lembrar as ultimas trocas entre lead e agente;
  - planner e reply passaram a receber `recent_history`, evitando que o agente se reapresente quando ja conversou com o lead;
  - perguntas sociais como "como voce esta?", "e voce?" e correcoes como "perguntei como voce esta" agora viram `reply_only/small_talk`, sem acionar estoque, anuncio ou fluxo de primeiro contato;
  - resposta social passa a responder primeiro a pergunta do lead, com tom humano e humilde quando errou a interpretacao;
  - deploy realizado em producao da Edge Function `pedro-webhook-v2`;
  - build frontend validado com `npm.cmd run build`.
- Hotfix do Pedro v2 para refinamento de estoque/fotos:
  - `pedro-webhook-v2` recebeu build `2026-05-26-photo-targeting-stock-format-v1`;
  - pedidos de fotos com atributos do lead, como "automatico", "manual", cor, ano ou carroceria, agora tentam escolher o veiculo correto entre os ultimos apresentados antes de cair na memoria antiga;
  - listas de estoque geradas pelo cerebro passaram a ter fallback deterministico com numeracao `1.`, `2.`, `3.` e linha `Foto:` quando a tool do estoque retorna imagem;
  - mensagens formatadas de estoque preservam quebras de linha, mas agora ainda enviam presenca de digitacao antes do texto;
  - pedidos de fotos passam a enviar uma frase curta antes das imagens, citando o veiculo/detalhe selecionado, e depois enviam as midias;
  - a ordem heuristica de fotos foi ajustada para tentar misturar frente/traseira/interior/painel com base na ordem do BNDV, sem alterar regras de transferencia/CRM;
  - build frontend validado com `npm.cmd run build` e deploy realizado em producao da Edge Function `pedro-webhook-v2`.
- Hotfix do Pedro v2 para leitura real de cards/anuncios do WhatsApp:
  - `pedro-webhook-v2` recebeu build `2026-05-26-uazapi-full-media-v2`;
  - tempo de download de imagem configurado com downloads completos via endpoint `/message/download`;
  - a visao do anuncio agora prioriza texto visivel do card;
  - build frontend validado com `npm.cmd run build` e deploy realizado em producao da Edge Function `pedro-webhook-v2`.
- Hotfix do Pedro v2 para atendimento consultivo de anuncio identificado:
  - `pedro-webhook-v2` recebeu build `2026-05-26-ad-vehicle-consultative-v3`;
  - limitacao do estoque ao primeiro match do anuncio para manter foco no carro anunciado;
  - build frontend validado com `npm.cmd run build` e deploy realizado em producao da Edge Function `pedro-webhook-v2`.
- Hotfix do Pedro v2 para busca estrita e audio:
  - `pedro-webhook-v2` recebeu build `2026-05-26-strict-stock-audio-v4`;
  - buscas estritas sem fallbacks com carros aleatorios;
  - transcreve audios usando Whisper / OpenAI API;
  - build frontend validado com `npm.cmd run build` e deploy realizado em producao da Edge Function `pedro-webhook-v2`.
- Hotfix do Pedro v2 para aceite de fotos:
  - `pedro-webhook-v2` recebeu build `2026-05-27-photo-offer-tool-v1`;
  - garante envio de fotos em lote e fechamento humano posterior;
  - build frontend validado com `npm.cmd run build` e deploy realizado em producao da Edge Function `pedro-webhook-v2`.

## Historico funcional recente consolidado

- Pedro recebeu varias correcoes em regras de transferencia, vendedor `ok`, CRM ao vivo, colunas e isolamento do agente.
- Marcos recebeu evolucoes em CRM manual, importacao/listas, campanhas, follow-up, instancias para vendedores, performance e exclusao em massa.
- Base teste/staging e scripts isolados configurados em producao e homologacao.
- Corrigidos bugs historicos de conexao com Supabase e fluxos Uazapi.
