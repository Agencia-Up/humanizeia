# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Supabase temp files
supabase/.temp/

# Local scripts and temp files
build.bat
tsc-check.bat
generate-doc.cjs
generate-report.cjs
humanizzei-preview.png
logos-preview.html
*.docx
Gemini_Generated_Image_*

# Source files de design (WhatsApp screenshots, removebg, etc.) — ficam só local
WhatsApp Image *.jpeg
WhatsApp Image *.jpg
WhatsApp Image *.png
WhatsApp_Image_*

# Claude local settings
.claude/settings.local.json

# Staging seed data (não commitado — é só pra teste local)
tests/staging-seeds/
.tmp-video/

# Segredos — NUNCA commitar. Os que já foram rastreados precisam de `git rm --cached` + ROTAÇÃO.
/.env
/secrets.txt
pedro_v2_architecture_and_credentials.md

# Scripts de diagnóstico locais — contêm service-role key hardcoded; NUNCA commitar.
scratch/
.tmp-*/
test_vision.cjs
