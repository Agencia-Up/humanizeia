# Handoff - F2.6I - preparacao de ativacao controlada do piloto real

Data: 2026-06-28
Executor: Claude (auditor: Codex)
Escopo: **somente documentacao/auditoria.** Nenhuma alteracao de codigo, banco, deploy ou ativacao.
`PEDRO_V3_PILOT_MODE` continua **OFF**. service_role NAO rotacionada.

Alvo do piloto: tenant `ecb26258-ffe6-4fe2-9efc-8ab2fc3a61b0` + agent `d4fd5c38-dd37-4da5-a971-5a7b7dfb9185` ("Aloan").

## 1. ENVs exatas (autoritativo, lido do codigo)

### A. Servico Pedro v3 (EasyPanel) — `Agent/src/runtime/server.ts`
Obrigatorias (o servico nao sobe sem elas — `requiredEnv`):
- `SUPABASE_URL` (https, sem user/senha)
- `SUPABASE_SERVICE_ROLE_KEY`
- `OPENAI_API_KEY`
- `PEDRO_V3_ALLOWED_UAZAPI_HOSTS` (CSV de hosts HTTPS permitidos da Uazapi; anti-SSRF) ← **estava ausente da lista da missao**
- `PEDRO_V3_BRIDGE_SECRET` (>=32 chars) ← **o codigo usa este nome, NAO `PEDRO_V3_SERVICE_SECRET`**

Opcionais (tem default):
- `PEDRO_V3_OPENAI_MODEL` (default `gpt-4.1-mini`)
- `PORT` (default `3000`)

### B. Webhook v2 (Supabase Edge Function `pedro-webhook-v2`) — `index.ts`
- `PEDRO_V3_PILOT_MODE` = `off` -> `shadow` -> `active` (NAO e env do servico EasyPanel; e da edge function)
- `PEDRO_V3_SERVICE_URL` (URL HTTPS publica do servico v3 no EasyPanel)
- `PEDRO_V3_BRIDGE_SECRET` (**identico** ao do servico — e o segredo Bearer compartilhado)

> ⚠️ Discrepancias vs a missao: (1) o nome correto e `PEDRO_V3_BRIDGE_SECRET` (nao `..._SERVICE_SECRET`);
> (2) `PEDRO_V3_ALLOWED_UAZAPI_HOSTS` e obrigatoria e faltava; (3) `PEDRO_V3_PILOT_MODE` pertence ao
> webhook, nao ao EasyPanel. O `README.md` do servico ja estava correto nesses pontos.

## 2. instance_id — BLOQUEIO FACTUAL (nao inventei instancia)

- O v3 le o **`instance_id` (singular)** do `wa_ai_agents` (`supabase-v2-read-adapter.ts:136`); `pilot-active-root.ts:140` faz `if (!instanceId) throw AGENT_WITHOUT_INSTANCE`. O campo `instance_ids` (array) **NAO** e lido.
- Estado real (consulta read-only):
  - `wa_ai_agents.instance_id` do Aloan = **NULL** -> piloto ativo falha fechado. **BLOQUEIO.**
  - `wa_ai_agents.instance_ids` = `{fdd6cbe1-cdf3-4806-b7c3-ef6bd184d03f}` -> essa instancia **NAO existe** em `wa_instances` (referencia ORFA; nao usar).
  - Instancia REAL conectada do piloto: **`6476a393-7ef6-4fba-9c7f-8d51ed966f58`** — nome `aloan`, provider `uazapi`, status `connected`, `is_active=true`, dona = tenant do piloto, `seller_member_id` nulo (linha do AGENTE, nao de vendedor), phone `558597895634`, ult. conexao 2026-06-29.
- Remediacao (write do DONO; eu nao altero banco). **So depois de o dono confirmar que `6476a393`/phone `558597895634` e a linha certa do piloto:**
  ```sql
  update public.wa_ai_agents
     set instance_id = '6476a393-7ef6-4fba-9c7f-8d51ed966f58'
   where id = 'd4fd5c38-dd37-4da5-a971-5a7b7dfb9185'
     and user_id = 'ecb26258-ffe6-4fe2-9efc-8ab2fc3a61b0';
  ```
  - `6476a393` passa nas checagens do v3 (ownership por tenant + provider uazapi + connected).
  - ⚠️ Antes de rodar, conferir como o **v2** roteia hoje a instancia `aloan` (com `instance_id` NULL ela pode nao estar amarrada ao agente no v2). Com `PEDRO_V3_PILOT_MODE` ainda `off`, mesmo apos setar o `instance_id` o webhook continua caindo no v2 (o gate registra o match e faz fallback) — entao setar agora e seguro e nao ativa nada.

## 3. Uazapi — evento `messages_update` na instancia piloto (R6)

- O v2 ja foi auditado (F2.6H): `message_update` e interceptado antes do `fromMe`, ignorado com seguranca p/ nao-piloto e nao polui inbox de vendedor. Logo, **habilitar `messages_update` na instancia `aloan` NAO quebra v2**.
- A instancia precisa que o webhook Uazapi inclua `messages_update` nos eventos. O path moderno
  (`create-uazapi-instance` -> `/webhook/instance`) ja inclui; o `sync-uazapi-webhook` (events
  `["messages","connection"]`) **removeria** — **nao re-sincronizar a instancia piloto por ele.**
- **Validacao (read-only, sem efeito):** consultar o webhook atual da instancia `aloan`:
  ```
  GET {api_url da instancia aloan}/webhook/find/aloan
  Header: token: {token da instancia aloan}
  ```
  Conferir se `messages_update` esta em `events`.
- **Sincronizacao PONTUAL (so a instancia piloto)** — preparada, **NAO executada** (precisa autorizacao + token, que o dono tem). Setar preservando o webhook URL atual + os eventos atuais, apenas ADICIONANDO `messages_update`:
  ```
  POST {api_url da instancia aloan}/webhook/instance
  Header: token: {token da instancia aloan}
  Body: { "url": "{URL atual do webhook v2}", "events": ["messages","messages_update","connection"], "excludeMessages": ["wasSentByApi"] }
  ```
  Nao foi criado script runnable commitado (evita execucao acidental); o dono roda manual (painel Uazapi ou curl) e confirma via o GET acima.

## 4. Checklist de ATIVACAO (ordem obrigatoria; nada disso foi executado)

Pre-requisitos (bloqueios factuais):
- [ ] SQL F2.6H ja aplicado (CONFIRMADO pelo dono: index_ok/function_ok=true).
- [ ] `wa_ai_agents.instance_id` do Aloan setado para `6476a393...` (secao 2) e confirmado.
- [ ] `messages_update` presente no webhook da instancia `aloan` (secao 3).
- [ ] service_role: decisao do dono/Codex sobre rotacao (hoje mantida; risco registrado).

Ativacao:
1. [ ] Deploy do servico v3 no EasyPanel (build context `services/pedro-v3`, Dockerfile, porta 3000) com as ENVs da secao 1.A e `PEDRO_V3_PILOT_MODE` NAO se aplica ao servico.
2. [ ] Healthcheck verde: `GET https://{servico}/health` = 200.
3. [ ] Deploy do webhook v2 build **`2026-06-28-pedro-v3-delivery-receipt-v221`** (sem auto-deploy; pelo fluxo padrao do dono).
4. [ ] No Supabase (edge function), setar `PEDRO_V3_SERVICE_URL` (URL do servico) e `PEDRO_V3_BRIDGE_SECRET` (identico ao do servico).
5. [ ] `PEDRO_V3_PILOT_MODE=shadow` PRIMEIRO. Smoke: mandar msg de teste pro numero do Aloan e conferir nos logs que o webhook registra o match do piloto e NAO duplica resposta; o v3 ingere em shadow sem despachar.
6. [ ] Smoke do receipt: confirmar entrega de uma msg e ver `messages_update` chegando ao webhook -> `/v1/pilot/receipt` -> outbox promovido a `delivered` (logs `[pedro-v3-receipt]`).
7. [ ] So depois de shadow OK: `PEDRO_V3_PILOT_MODE=active`. Validar 1 conversa real do Aloan ponta-a-ponta (resposta unica, sem repetir pergunta, foto nao re-enviada).

## 5. ROLLBACK (imediato, sem perda)

- **Reversao instantanea:** setar `PEDRO_V3_PILOT_MODE=off` na edge function. O webhook volta 100% ao v2 para o Aloan (o gate so adiciona um ramo; off = comportamento v2 puro). Nenhum outro cliente e afetado em momento algum.
- Se o servico v3 cair: com `off`/`shadow` o v2 ja responde; em `active`, o contrato anti-resposta-dupla faz o v2 assumir apenas quando o v3 declara `ingested=false` (timeout/falha pos-insert = incerto, sem dupla). Reverter para `off` encerra qualquer duvida.
- Reverter o webhook: redeploy do build anterior (`...active-bridge-v220`) se necessario.
- Reverter `instance_id`: `update ... set instance_id = NULL ...` (volta ao estado atual).
- NAO e necessario tocar no banco v3_* para rollback (nenhum efeito destrutivo foi ligado).

## 6. Comandos que o DONO/Codex devem rodar (resumo)

- (Codex/gates, ja verdes na F2.6H) `cd services/pedro-v3/Agent && npm run test:all` · `npx tsc --noEmit` · `npx tsx scripts/regression/offline.ts` · bundle webhook esbuild.
- (Dono, pre-ativacao) SQL do `instance_id` (secao 2, apos confirmar a linha) · GET `/webhook/find/aloan` + POST `/webhook/instance` pontual (secao 3) · setar ENVs EasyPanel + edge function · deploy v3 + webhook v221 · `PEDRO_V3_PILOT_MODE=shadow` -> smoke -> `active`.

## 7. Riscos restantes

- **R-1**: `instance_id` NULL (bloqueio) + `instance_ids` aponta p/ instancia orfa (`fdd6cbe1`). A instancia real e `6476a393`. Risco se alguem setar a orfa por engano — por isso o SQL acima fixa a correta.
- **R-2**: `sync-uazapi-webhook` removeria `messages_update`. Nao re-sincronizar a instancia piloto por ele.
- **R-3**: service_role exposta nao rotacionada (mantida por decisao; bloqueia hardening final, nao a mecanica do piloto).
- **R-4**: roteamento v2 do Aloan apos setar `instance_id` — conferir que nao muda comportamento atual indesejado (com PILOT_MODE off, segue no v2).

## 8. O que NAO foi feito (por instrucao)

- Nenhuma alteracao de codigo (envs/Dockerfile/server ja corretos; README so recebeu secoes operacionais).
- Nenhum deploy, `db push`, migracao ou ativacao. `PEDRO_V3_PILOT_MODE` OFF.
- service_role nao rotacionada. Nenhuma instancia inventada. Nenhum script runnable commitado p/ Uazapi.
- CRM-write/handoff/agenda fora desta rodada.

**Parado para auditoria do Codex (F2.6I).**
