# Handoff — 2026-06-28 — Claude → Codex (F2.5.2A: contratos read-only + config)

## 1. Escopo entregue (só F2.5.2A)

Contratos read-only + carregamento seguro de config do tenant/agente + `CredentialProvider`/`SecretRef` + fakes.
**Sem HTTP/rede, sem RevendaMais/BNDV reais, sem CRM, sem QueryRunner, sem LLM. Kernel, v2 e banco intocados.**
**Não iniciei B, C nem D.**

## 2. Arquivos

**Criados**
- `Agent/src/domain/read-ports.ts` — `TenantAgentRef`, `TenantRuntimeConfig` (readonly), `PromptSource`, `StockProvider`/`SelectedStockProvider`, `ReadConfigError(Code)`, `ConfigResult`, `TenantConfigSource`.
- `Agent/src/domain/credential-provider.ts` — `SecretRef` (`{tenantId,integrationId,provider,purpose}`), `CredentialProvider`, `makeSecretRef` (allowlist dos 4 campos), `ResolvedSecret`, `SECRET_KEY_DENYLIST`.
- `Agent/src/adapters/read/v2-read-gateway.ts` — `V2ReadGateway` (métodos específicos: `getOwnedAgent`, `getOwnedFunnelConfig`, `listActiveStockIntegrationMetadata`), rows tipadas (`model`/`temperature` = `unknown` p/ validar), `V2ReadGatewayError`, `assertTenantAgentRef`. Sem impl. real (Supabase só em B/C).
- `Agent/src/adapters/read/tenant-config-source.ts` — `V2TenantConfigSource`.
- `Agent/src/adapters/read/fakes/fake-v2-read-gateway.ts` — fake determinístico; guarda segredos mas nunca expõe.
- `Agent/src/adapters/read/fakes/fake-credential-provider.ts` — spy (`resolveCount`).
- `Agent/tests/run-read-side.ts` — 28 checks.

**Alterados**
- `Agent/package.json` — `test:read` + incluso em `test:all`.
- `Brain/01-STATUS-ATUAL.md`, `Brain/08-PLANO-F2.5.2-READ-SIDE.md` (R1 autoritativa + registro F2.5.2A + DEPRECATED nos trechos antigos), este handoff.

## 3. Contratos obrigatórios — como ficaram

1. **Tenant+agente explícitos** (`TenantAgentRef`); sem email/instanceId/"primeiro agente ativo".
2. **Gateway específico** (sem tabela/filtro livre); cada método valida tenant + propriedade; `assertTenantAgentRef` rejeita ref incompleto.
3. **`TenantRuntimeConfig` readonly** com tenantId/agentId/agentName/companyName(""→null)/instanceId/promptText/promptSource/model/temperature/sdrGoal/qualificationQuestions/sellsMotorcycles/blockedCategories/ragRestricted/`stockProvider`/`stockSecretRef`/`versionStamp`. **Sem credencial.**
4. **Prompt**: `use_funnel_config=false`→`system_prompt`; `true`→`generated_system_prompt`; vazio→`PROMPT_SOURCE_EMPTY` (sem fallback; conteúdo do prompt nunca em erro/log).
5. **Estoque**: RevendaMais vence BNDV por metadata; só `integração/provider/SecretRef opaco`; sem credencial; **sem fallback silencioso** (só-BNDV→bndv; nenhum→none).
6. **`SecretRef`** = 4 campos opacos; sem feed URL/token/api_key.
7. **`CredentialProvider`** separado; **não** chamado no load (spy prova `resolveCount=0`); erro nunca inclui segredo.
8. **Config inválida** → erros tipados (`AGENT_NOT_FOUND` p/ inexistente E cross-tenant; `AGENT_INACTIVE`; `PROMPT_SOURCE_EMPTY`; `INVALID_MODEL`; `INVALID_TEMPERATURE`; `PROVIDER_METADATA_INCONSISTENT`; `MISSING_TENANT_OR_AGENT`). Sem default comercial inventado.

## 4. Testes (28) — cobrem a lista mínima + adversariais da fatia

tenant carrega só o seu agente · cross-tenant fail-closed · tenant/agentId obrigatórios · inativo rejeitado · companyName ""→null · prompt cru · prompt funil · funil vazio e system_prompt vazio → tipado · prompt não vaza em erro · RevendaMais vence BNDV · só-BNDV→bndv e ambos nunca caem em BNDV (sem fallback) · config sem chave/valor de credencial (canário) · SecretRef só 4 campos · CredentialProvider não chamado · model/temperature inválidos (3 casos) · fakes determinísticos · gateway rejeita consulta sem tenant/agente · provider duplicado→inconsistente · sem estoque→none.

## 5. Gates

- `cmd /c npm.cmd run test:all` → **242 OK | 0 FALHA** (214 existentes intactos + 28 novos).
- `npm.cmd exec -- tsc --noEmit` → limpo (exit 0).
- `rg` nas fontes da fatia: **sem** fetch/http, Uazapi, EffectDispatcher, escrita CRM, import do Pedro v2, `@supabase`/postgres. Strings `https://…`/`token` aparecem **só no teste** (canários anti-vazamento, não há `fetch(`).

## 6. Próximo passo

1. **Codex audita a F2.5.2A.**
2. Aprovado → **F2.5.2B** (estoque + identidade `source+externalVehicleId`/fingerprint + categoria typed + catálogo dinâmico + fotos `photoId` estável + adapter HTTP com controles R1-5), mantendo verdes + `tsc` limpo, parando para auditoria.

## 7. Observação para B (não feito agora)

O `V2ReadGateway` real (Supabase) e o `stock-source` (fetch read-only com allowlist/anti-SSRF/timeout) entram em B. Em B será preciso confirmar, por inspeção read-only controlada, o **id externo real** (RevendaMais/BNDV) e eventual campo de carroceria — antes de definir `vehicleKey`/`VehicleType` (R1-1/R1-4).

---
**Parado para auditoria do Codex (F2.5.2A).**
