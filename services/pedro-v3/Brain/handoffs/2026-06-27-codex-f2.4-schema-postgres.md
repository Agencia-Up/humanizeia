# Handoff - F2.4 Schema PostgreSQL/Supabase

Data: 2026-06-27  
Autor: Codex

## Entrega

- `Brain/sql/v3_schema.sql`: schema completo e executavel, sem tocar o v2.
- `Brain/sql/v3_verify_after_install.sql`: auditoria somente leitura para rodar depois da instalacao.
- `Brain/decisions/ADR-007-mapeamento-postgres-v3.md`: mapeamento dos ports para tabelas/RPCs.
- `Agent/tests/run-sql-schema.ts`: integracao real contra PostgreSQL embutido.
- `Agent/package.json`: scripts `test:sql` e `test:all` ampliado.
- Dependencia de desenvolvimento `@electric-sql/pglite` para validar DDL/RPCs localmente.

## Garantias verificadas

1. Ingestao atomica e dedupe pelo proprio insert.
2. Lease exclusivo e claim de inbox condicionado ao lease.
3. Commit do turno atomico com CAS, sem persistencia parcial.
4. Outbox com IDs estruturais imutaveis, grafo valido e claim concorrente por token.
5. `accepted` critico nao avanca memoria; upgrade assincrono para `delivered` e suportado.
6. Outcome entregue aplica estado uma vez; repeticao e no-op.
7. `outcome_applied_at` e `terminal_at` possuem significados separados.
8. Cofre sensivel nao tem leitura para `authenticated` e exige ciphertext.
9. RLS ativo e forcado em todas as tabelas.

## Proximo passo

O dono executa `v3_schema.sql` no SQL Editor do Supabase e, depois, executa `v3_verify_after_install.sql`. Somente apos o Codex verificar o resultado com todas as linhas `ok=true` a F2.5 (adapter Postgres real) deve comecar.

Nenhum adapter real, provider, webhook, banco remoto ou deploy foi acionado nesta fase.
