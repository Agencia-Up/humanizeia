# Handoff — F2.7.7 Captura segura de slots + objetivos (+ auditoria de fotos)

Data: 2026-06-30
Executor: Claude (auditor: Codex). Gates verdes. Sem CRM/handoff/follow-up. Sem SQL. v2/bridge/webhook intactos. **Parar p/ auditoria do Codex antes de CRM/handoff.**

## Problema (aprovado pelo Codex)
O v3 agrupava a rajada e respondia estoque, mas NAO capturava slots: o LLM segue `facts:[]` (travado na F2.6R p/ estabilizar o envelope) e o engine so injetava `append_lead_turn`. Resultado real: "dOUGLAS / Quero um onix / ou argo" -> respondia Onix mas **nao salvava o nome, reperguntava o nome, e ignorava o Argo**.

## Correcao — camada DETERMINISTICA (regras 1/2: NAO religar facts pro LLM)
- **`engine/lead-extraction.ts` (NOVO, PURO):** `extractLeadSlots()` transforma a fala do lead em `DecisionMutation[]` VALIDAS. O LLM continua com `facts:[]`; o engine injeta as mutacoes do extrator (mesma fonte unica do `append_lead_turn`). So emite o que o reducer aceita -> nunca derruba o turno (o reducer rejeita o lote inteiro se algo invalido, por isso o extrator e conservador).
  - **NOME (regra 3):** padrao explicito ("meu nome é/me chamo/sou o X" — `\p{L}` unicode, aceita acento) OU **sinal de que o nome foi pedido** [objetivo de nome pendente OU o AGENTE acabou de perguntar o nome na ultima fala dele] + linha com 1-3 tokens "limpos" (alfabetico, fora da stoplist de palavras comuns, nao-veiculo via claimExtractor). ⚠️**Em prod o objetivo raramente existe (LLM nao emite planned), entao o sinal-chave e a PERGUNTA DO AGENTE** (`AGENT_ASKED_NAME` na ultima fala) — e o que faz o "dOUGLAS" pelado ser capturado. Normaliza deterministicamente: "dOUGLAS" -> "Douglas". Confianca 0.95 (padrao) / 0.85 (sinal+token); piso 0.7. Sem padrao e sem sinal -> NAO captura (ambiguo).
  - **INTERESSE multi-modelo (regra 4):** modelos citados (claimExtractor do catalogo + interpretacao) -> `slots.interesse` em FORMATO DOCUMENTADO (lista normalizada unida por ", ", ex.: "onix, argo"); faz UNIAO com o existente (NUNCA apaga um pelo outro). **Sem novo contrato de estado** (regra 9) — usa o slot string existente.
  - **OBJETIVO (regra 5):** se `currentObjective.slot="nome"` pendente e capturamos o nome -> `resolve_objective` satisfied (objectiveId real).
- **`engine/conversation-engine.ts`:** alem de injetar as mutacoes no commit, aplica o extrator num **ESTADO-PREVIA** (sem bump de version/turnNumber) ANTES de decidir -> `deriveModelContext` ja mostra `nome`/`interesse` capturados + objetivo resolvido -> o modelo **reconhece e NAO repergunta o nome no MESMO turno** (era a falha visivel do print).

## Responder o BLOCO inteiro (regra 6)
- `domain/decision.ts`: `TurnInterpretation.extractedEntities.models?: string[]` (ADITIVO; `model` segue p/ compat).
- `openai-chat-model.ts`: interpret extrai TODOS os modelos do bloco (`models`); compose responde os encontrados (offer_list) e diz "nao encontrei X" pros que faltam (sem dropar nenhum).
- `decision-engine.ts`: o PRE-SEED consulta cada modelo do bloco (`models[]` + claims do catalogo; teto 3) -> Onix e Argo ambos consultados.
- `prompt-bound-conversation.ts`: decoder valida `models[]`.

## Testes — `tests/run-f2-7-7-slots.ts` (NOVO, 17 OK)
"dOUGLAS"+obj -> nome "Douglas"+resolve; "Meu nome é douglas" -> "Douglas"; "Conheço sim" NAO vira nome; "Quero um onix" NAO vira nome (vira interesse); bloco completo -> nome+resolve+interesse(onix,argo); sem objetivo -> sem resolve; nome ja conhecido -> nao recaptura + aparece em conversationFacts; ambiguo -> nao salva. **Engine e2e:** bloco real -> nome salvo, interesse onix+argo, **preview faz o modelo ver o nome (status known)**, pre-seed consulta onix E argo.

## Gates
`npm run test:all` **EXIT=0** (F2.7.7 17 + todas as demais). `npx tsc --noEmit` limpo. Scan limpo (extrator e PURO, sem I/O). Sem SQL, sem mudanca de contrato de estado/tabela (rule 9). v2/bridge/webhook intactos (rule 10).

## Auditoria do fluxo de FOTOS (pedido do Codex) -> vira F2.7.8
- **JA EXISTE (infra completa):** query `vehicle_photos_resolve` (read-query-runner -> resolvePhotos {vehicleKey,ambiguous,photoIds}); materializacao `send_media` (effect-materializer); dispatch `send_media` via uazapi (whatsapp-dispatcher); `mark_photos_sent` + `photoLedger.sentByVehicle` (state-reducer, com midia parcial por perItem); tipo `SendMediaPlan`; `proposedAction:"send_photos"`; decoder aceita o efeito send_media (validEffect).
- **ONDE QUEBRA (raiz):** o **prompt do `propose` so descreve o efeito `send_message`** — NUNCA descreve `send_media`. Entao o modelo, ao receber "manda foto", ate roda `vehicle_photos_resolve`, mas **nao tem como emitir o efeito de midia** -> responde so texto ("fingindo"). Era a falha do dono.
- **FALTA:** (1) prompt do propose descrever `send_media` (apos resolver fotos, emitir send_media com vehicleKey+photoIds); (2) logica de decisao: resolver o veiculo correto (foco/lista) antes de pedir foto; (3) ledger anti-reenvio (nao reofertar/reenviar foto ja enviada sem pedido explicito); (4) caminho HONESTO "nao encontrei fotos desse agora" (sem marcar enviado, sem repetir CTA "quer fotos?"); (5) testes do fluxo.
- **DECISAO:** bloco coeso e nao-trivial (prompt + decisao + ledger + honestidade + testes). Vira **F2.7.8** (Codex permitiu), sem misturar com slots.

## Criterio de aceite (WhatsApp, pos-deploy)
Rajada "dOUGLAS / Quero um onix / ou argo": o agente salva o nome (Douglas), considera Onix E Argo, e **NAO** pergunta "qual seu nome?".

**Parado para auditoria do Codex (F2.7.7). Proxima proposta: F2.7.8 = fluxo de fotos.**
