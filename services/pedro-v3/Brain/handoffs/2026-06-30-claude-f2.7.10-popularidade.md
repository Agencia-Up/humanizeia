# Handoff — F2.7.10 Popularidade honesta + coerencia de terminal-safe (diagnostico do print "mais populares")

Data: 2026-06-30
Executor: Claude (auditor: Codex). Gates verdes. Sem CRM/handoff/follow-up. Sem SQL. v2/bridge/webhook intactos. **Parar p/ auditoria do Codex antes de CRM/handoff.**

## Diagnostico read-only (banco, conversa pos-reset; 7 turnos poll-1..7)
O print sugeria "mais populares -> lista de baratos + terminal-safe no mesmo turno". O banco **corrige** isso:
- **poll-6** lead="Certo, quais os carros mais baratos que tem?" (19:28:30) -> `inform_cheapest_cars` -> lista de baratos (**CORRETO**, a F2.7.9 funcionou). dispatch 19:28:45.
- **poll-7** lead="mais populares" (19:28:37) -> **`terminal_safe`**. dispatch 19:28:54.
- **Sao 2 TURNOS DIFERENTES** (lead messages diferentes), 9s entre os dispatches. NAO foi um turno que produziu lista + terminal-safe.

### Respostas as 7 perguntas do dono
1. **Quantos turnos:** "mais baratos" e "mais populares" viraram 2 turnos (poll-6, poll-7) — chegaram com **7s** de intervalo, fora da janela de 6s. A conversa toda = 7 turnos.
2. **Lista e terminal-safe: mesmo turn_id?** NAO — poll-6 (lista) vs poll-7 (terminal-safe).
3. **Causa do terminal-safe:** **policy deny** `POL-GROUND-PRICE` -> *"vehicle_offer_list: veiculo 'revendamais:8106339' nao encontrado nos fatos do turno"*. (NAO foi timeout/compose-invalid/query-error.) O LLM tentou ofertar uma lista de "populares" SEM fatos -> chave ungrounded -> deny -> apos re-tentativas -> terminal-safe. **Mesmo mecanismo do bug "baratos" (F2.7.9), mas para popularidade — que nao tem dado real.**
4. **Debounce segurou 6s?** SIM. Agrupou "Sou sim"+"Queria um onix" (poll-2) e "Tenho nao"+"Me manda fotos" (poll-5). "baratos"(:30)+"populares"(:37) ficaram 7s -> corretamente separados.
5. **/v1/pilot/turn sync?** NAO — so ingere; TODOS os turnos sao "poll-N" (processados pelo poller). Sem caminho sincrono.
6. **Mais de um poller?** Sem evidencia — turnos sequenciais poll-1..7, cada evento `claimed` 1x, sem processamento concorrente da mesma conversa.
7. **Terminal-safe DEPOIS de resposta valida no MESMO turno?** NAO. Logo, **NAO ha bug critico de engine/outbox**. O engine produz UMA decisao por turno; terminal-safe CANCELA efeitos comerciais (nao duplica).

## ⭐ CORRECAO DE DOMINIO (Codex/dono): "carro popular" no Brasil = ENTRADA/economico, NAO ranking
A 1a versao tratou "populares" como ranking -> honesto. Errado p/ usados BR. Revisado em `engine/popularity-intent.ts` (NOVO), DUAS intencoes:
- **ECONOMY** — `detectPopularEconomyIntent` (`/\bpopular(?:es)?\b/`): "populares", "carros/modelos populares", "mais populares" -> oferta REAL do estoque economico. `resolvePopularEconomyOffer` roda `stock_search({broad:true})` (a fonte ordena por preco asc) e pega as **5 mais em conta**; `buildPopularEconomyTurnOutput` renderiza `renderVehicleOfferList` (ancorado nos VehicleFact reais) com NOTA do criterio: *"Quando voce fala em populares, vou considerar as opcoes mais de entrada e economicas. Separei algumas do nosso estoque:"*. Estoque vazio -> honesto. **Nunca inventa ranking; nunca terminal-safe** (deterministico, chaves vindas do estoque).
- **RANKING** — `detectPopularityRankingIntent` (mais vendidos/procurados/o que mais sai/best sellers/campeoes de venda): SEM fonte real -> `buildPopularityRankingTurnOutput` honesto *"Nao tenho um ranking real de vendas por aqui, entao nao vou inventar. Mas posso filtrar por mais em conta / automaticas / SUVs / faixa de valor…"*.
- `conversation-engine` (ordem): photo -> **economy** (oferta) -> **ranking** (honesto) -> runTurn. "mais baratos" segue na via de PRECO (F2.7.9); combo "populares e baratos" -> economy (oferta real, sem ranking inventado).
- ⚠️Sem lista hardcoded: a oferta final SEMPRE vem do `stock_search` real (a lista de "populares BR" nao foi usada nem como filtro nesta versao — pode ser adicionada depois SO p/ priorizacao, mantendo a oferta ancorada no estoque). Teto ("populares ate 50 mil"): o sistema ainda nao extrai teto -> busca economico sem inventar teto (revisitar quando o slot de faixa existir).

## Tarefa 2 — coerencia de terminal-safe (defense + testes)
O engine ja garante 1 decisao/turno e terminal-safe CANCELA comercial (SAFE_RESPONSE). Cobri por teste: oferta **grounded -> NAO terminal-safe**; oferta **ungrounded -> terminal-safe SO com fallback** (sem send_media/comercial). NAO existe path de "valida + terminal-safe no mesmo turno" (o problema do print eram 2 turnos).

## Tarefa 4 — debounce: OK, sem mexer
Funciona (agrupa < 6s; 7s -> separado). NAO aumentei o tempo no chute (sem evidencia de processamento cedo). Default 6000ms mantido.

## Testes — `tests/run-f2-7-10-popularity.ts` (NOVO, 44 OK) + `test:f2710`
detect ECONOMY (populares/carros populares/modelos populares -> true; vendidos/baratos/onix -> false); detect RANKING (vendidos/procurados/o que mais sai/best sellers/campeoes -> true; populares/baratos -> false); "mais baratos" segue broad-price (F2.7.9); `resolvePopularEconomyOffer` (5 mais em conta, exclui o caro, vazio->none); build economy (nota entrada/economicas + lista ancorada Uno+preco; nao terminal-safe; so send_message); build ranking honesto; **e2e:** "mais populares" -> stock_search(broad)+send_message, ZERO send_media; "carros populares e baratos" -> idem; "mais vendidos" -> SEM stock_search, honesto; **T2:** grounded->nao terminal-safe, ungrounded->terminal-safe so com fallback.

## Gates
`npm run test:all` **EXIT=0** (F2.7.10 26 + F2.7.9 23 + F2.7.8 47 + todas). `tsc` limpo. Sem SQL, sem mudar contrato de estado. v2/bridge/webhook intactos.

**Parado para auditoria do Codex. NAO empurrado. Nao avancar p/ CRM/handoff.**
