# Handoff — 2026-06-28 — Claude → Codex (auditoria read-side + plano F2.5.2)

## 1. Objetivo

Auditoria **read-only** do Pedro v2 + portal para descobrir as fontes VIVAS de leitura e entregar o plano da F2.5.2. **Nada implementado.** Agent/, v2 e banco intocados.

## 2. Baseline reconfirmado

`npm run test:all` → **214 OK | 0 FALHA** (67 kernel + 92 fase2 + 34 SQL + 21 postgres adapter); `tsc --noEmit` limpo.

## 3. Fontes vivas (verificadas por import/query/runtime)

| Capacidade | Fonte viva | Evidência |
|---|---|---|
| Config + prompt | `wa_ai_agents` (+`agent_funnel_config`) | `pedro-webhook-v2/index.ts:329` `selectActiveAgent`; prompt cru `system_prompt` OU `generated_system_prompt` se `use_funnel_config` |
| Estoque (externo) | `platform_integrations` → RevendaMais feed / BNDV GraphQL | `stockSearch_20260525_photo_flow.ts::searchPedroStock` (revendamais precede bndv) |
| Fotos | campo `pictureJs` do item (`[{Link,Principal}]`) | `revendaMaisStock.ts` / `parseBndvPictures` |
| CRM | `ai_crm_leads` | colunas confirmadas (lead/cliente/qualificação + cpf/birth_date sensíveis) |
| KB | `agent_knowledge_bases`/`knowledge_bases`/`knowledge_chunks` | `knowledgeBase.ts` |

## 4. Agente de teste (`douglasaloan@gmail.com`) — por SQL

- tenant `user_id = ecb26258-ffe6-4fe2-9efc-8ab2fc3a61b0`; agente **"Aloan"** `agent_id = d4fd5c38-dd37-4da5-a971-5a7b7dfb9185` (ativo).
- `instance_id = NULL`; `use_funnel_config=false` (prompt cru); `company_name=""`; estoque BNDV+RevendaMais ativos (RevendaMais vence).

## 5. Plano entregue

`Brain/08-PLANO-F2.5.2-READ-SIDE.md` (16 pontos): inventário factual; mapa fonte→`QueryInputMap/QueryOutputMap`; ports/adapters read-only (`read-ports.ts`, `adapters/read/*`, `engine/query-runner.ts`); `TenantRuntimeConfig`; hierarquia do prompt (Kernel/policies > tenant > prompt); `TenantCatalog` dinâmico do estoque; `stableVehicleKey`; paginação/`excludeKeys`/"mais opções"; redaction de CPF + isolamento por tenant; timeouts/erros tipados/fallback; cache tenant-scoped; testes fake→integração→shadow; critérios de saída; arquivos exatos; riscos/dúvidas.

## 6. Divergências registradas (código venceu o doc)

(a) agente "Aloan" (não "Sara"); (b) `instance_id` NULL (bloqueio só da fase ativa); (c) 2 estoques ativos → precedência RevendaMais; (d) prompt cru (`use_funnel_config=false`); (e) **estoque/fotos = HTTP externo** → read-side exige fetch read-only (≠ EffectDispatchers, que seguem OFF).

## 7. Decisões do dono pendentes (§16 do plano)

1. Aceitar fetch HTTP read-only para estoque/fotos (RevendaMais/BNDV)?
2. Conectar a instância do agente de teste antes da fase ativa (não bloqueia shadow).
3. Manter precedência RevendaMais ou desativar BNDV para o tenant de teste?
4. Prompt do piloto: `system_prompt` cru ou ligar o funil?
5. Fronteira de reuso da lógica do v2 (rank/subcategoria/excludeKeys/fotos): adaptador/cópia auditada, sem import direto (ADR-005).

## 8. Restrições respeitadas

Sem alterar Agent/, v2 ou banco; sem migration/SQL remoto/db push; sem provider/WhatsApp/CRM/handoff/deploy; Supabase/MCP só-leitura; só `Brain/` atualizado (plano + status + este handoff).

## 9. Próximo passo

1. **Codex audita o plano `08`** + dono responde §16.
2. Aprovado → implementar a F2.5.2 em fatias (read-ports → tenant-config → stock/photos → crm → query-runner → testes fake/integração/shadow), mantendo 214 verdes + `tsc` limpo, parando para auditoria a cada fatia.

---
**Parado para auditoria do Codex.**
