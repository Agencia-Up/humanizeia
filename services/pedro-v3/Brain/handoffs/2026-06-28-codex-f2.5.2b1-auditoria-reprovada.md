# Handoff - Auditoria Codex F2.5.2B.1

Data: 2026-06-28
Resultado: **reprovada; F2.5.2C bloqueada**

## Incidente critico

Foi encontrada uma chave Supabase `service_role` completa hardcoded em `Agent/scratch/inspect_real_sources.mjs`. O arquivo nao esta no `.gitignore`, faz leitura ampla de integracoes e imprime payloads brutos. O Codex nao executou o script nem reproduziu o segredo. A chave deve ser considerada comprometida e rotacionada/revogada pelo dono antes de qualquer nova integracao. Depois, remover o segredo do workspace e verificar logs/historicos externos onde o script possa ter sido exibido.

## Gates

- `test:all`: 294 OK, 0 falhas (nao 300+).
- `tsc --noEmit`: limpo.
- Listas manuais por modelo foram removidas e nenhum efeito externo foi ligado.

## Provas adversariais do Codex

- Catalogo construido com `revendamais:101` possui uma entrada, mas `isVehicleKeyInCatalog(..., revendamais:101)` retorna false. O Kernel bloquearia o estoque real.
- Apos `invalidate`, uma segunda leitura antes da Promise antiga terminar nao chama o fetch novo e recebe o valor stale.
- O decoder aceita objeto em `vehicle_id`/`make` e produz `[object Object]` em vez de rejeitar.

## Bloqueios B.2

1. Rotacionar/revogar a service_role e remover qualquer segredo hardcoded. Nunca usar service_role em script de inspecao.
2. Unificar o contrato de vehicleKey do catalogo/policy; adicionar teste end-to-end buildCatalog -> PolicyEngine.
3. Corrigir cache com token por flight: invalidate deve impedir novos callers de compartilharem flight stale; conclusao antiga nao pode apagar flight novo.
4. Decoder deve aceitar somente tipos/limites explicitamente permitidos; objetos/arrays falham fechado.
5. StockLoader deve validar agente+tenant e reutilizar a selecao validada do TenantConfigSource, sem duplicar precedencia/metadata.
6. Entregar evidencia sanitizada real. O script encontrado nao consulta os campos novos do BNDV e nenhum resultado sanitizado foi salvo.
7. Implementar testes ausentes: timeout, retry so GET, TTL, LRU, single-flight, cross-tenant/agente e invalidacao concorrente completa.
8. Mapear erros HTTP por codigos fechados; nunca propagar message arbitraria de stream/redirect.
9. Criar adapters reais read-only de V2ReadGateway/CredentialProvider apenas em fatia autorizada; hoje so existem contratos/fakes.
10. Atualizar Brain e criar handoff real. A B.1 nao atualizou status nem criou handoff.

Sem banco, deploy, WhatsApp, CRM write, handoff, LLM real ou F2.5.2C/D. Parar novamente para auditoria.