# Handoff — 2026-06-28 — Claude → Codex (revisão R1 do plano F2.5.2)

## 1. Objetivo

Direção da F2.5.2 aprovada; implementação NÃO. Incorporar as **12 correções obrigatórias** ao `Brain/08` e parar. **Agent/ intocado; nada implementado; v2 e banco read-only.**

## 2. Investigação read-only adicional (código vivo do v2)

- **RevendaMais** (`revendaMaisStock.ts`): `RawRevendaVehicle = Record<string,any>`; normalização **descarta id externo**; `category` = AUTOMOVEL/MOTO (classe, não carroceria); fotos `images_large/images` → `pictureJs [{Link,Principal}]`; `year`/`saleValue` podem ser null.
- **BNDV** (`stockSearch_…::searchPedroStock`): GraphQL pede só campos básicos, **sem id e sem carroceria**.
- **Carroceria** = `getVehicleSubcategory` = **regex de listas manuais** → hatch/sedan/suv/pickup/unknown (NÃO copiar).
- **Fotos** = `parseBndvPictures` → `{url, principal}`; URL pode ser assinada/temporária.

## 3. Correções aplicadas no `08` (seção REVISÃO R1)

1. Identidade: `source+externalVehicleId` (confirmar campo real por inspeção gated em B); fallback fingerprint de atributos **não-voláteis** (nunca índice/preço/km); colisão → `ambiguous=true`, sem foto automática.
2. Fotos: `photoId` = hash do **path estável** (sem query/assinatura) escopado ao vehicleKey; resolúvel pós-restart/re-assinatura; foto nunca cruza veículos.
3. Preço/ano ausentes: fail-closed (sem preço → não entra em oferta firme, "a confirmar", nunca R$0/inventado); mudança de `VehicleFact` → **mini-ADR + parada antes**.
4. Categoria: `VehicleType` com `{confidence, provenance}`; `unknown` nunca vira SUV; classificador principiado (não a regex do v2); `broad` não relaxa rígido.
5. Fetch: HTTPS/allowlist por provider/anti-SSRF (localhost/IP privado/metadata)/redirects revalidados/limite de bytes/content-type/AbortSignal/retry-só-GET/sem credencial em log; LLM nunca escolhe URL/host/cred.
6. Credenciais: `CredentialProvider`/`SecretRef`; `api_key_encrypted` nunca em config/estado/QueryResult/evento/log.
7. CRM: sem `cpf`/`birth_date`; `leadId` = UUID `ai_crm_leads.id`; exige `tenantId+agentId+leadId` + valida propriedade.
8. Tenant/agente: explícitos; verifica que o agente pertence ao tenant; proibido "primeiro agente ativo"; email só descoberta.
9. KnowledgeSource: removida do escopo (sem `knowledge_search` no contrato) — proposta separada se desejada.
10. Prompt: `system_prompt` carregado/validado, sem alegar integração até LLM ligado; policies vencem o prompt.
11. Cache: Clock injetável + LRU + single-flight por (tenant,provider); TTL é a base (não depender de `last_sync_at`); testes HTTP reais fora do `test:all`.
12. Testes adversariais: cross-tenant, SSRF, redirect IP privado, feed malformado, credencial-fora-de-log, 2 unidades mesmo modelo/ano, colisão fingerprint, preço/ano ausentes, SUV com unknown, excludeKeys, foto não cruza, cache isolado, broad não relaxa.

## 4. Slices + decisões

- **F2.5.2A** contratos+config+fakes → **B** estoque/identidade/categoria/catálogo/fotos+HTTP seguro → **C** CRM+QueryRunner → **D** harness shadow. Cada uma: testes+tsc+handoff+parada.
- Decisões do dono aplicadas: fetch read-only OK (com controles); não conectar instância; RevendaMais principal; **sem fallback silencioso p/ BNDV** se RevendaMais falhar (erro tipado + fallback seguro); prompt cru; não importar v2/listas.

## 5. Próximo passo

1. **Codex audita a revisão R1** do `08`.
2. Aprovado → implementar **F2.5.2A** (contratos read-only + CredentialProvider + TenantConfigSource + fakes), mantendo 214 verdes + `tsc` limpo, parando para auditoria.

---
**Parado.** Brain atualizado (`08` REVISÃO R1, `01`, este handoff). Agent/ intocado.
