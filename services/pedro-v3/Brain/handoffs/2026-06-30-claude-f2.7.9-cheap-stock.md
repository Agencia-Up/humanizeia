# Handoff — F2.7.9 Busca AMPLA por preco baixo ("modelos baratos") -> lista real, nunca terminal-safe

Data: 2026-06-30
Executor: Claude (auditor: Codex). Gates verdes. Sem CRM/handoff/follow-up. Sem SQL. v2/bridge/webhook intactos. **Parar p/ auditoria do Codex antes de CRM/handoff.**

## Problema (teste real, print do dono)
Lead: "Quais modelos baratos você tem?" -> agente caiu em **terminal-safe** ("Desculpe a lentidão temporária...").
Diagnostico read-only no banco (poll-5): `reason_code=terminal_safe`, razao = `POL-GROUND-PRICE` deny -> *"Erro de renderização: vehicle_offer_list: veículo 'revendamais:8186724' não encontrado nos fatos do turno"*.
**Raiz:** o pre-seed de estoque (F2.7.4-D) so roda `stock_search` quando o lead NOMEIA um modelo (`detectRequestedModels`). "modelos baratos" nao nomeia modelo -> **nenhum stock_search** -> o `vehicle_offer_list` do LLM citou um vehicleKey FORA dos fatos -> grounding deny -> apos re-tentativas -> terminal-safe.

## Correcao — pre-seed de busca AMPLA por preco (em `engine/decision-engine.ts`)
Invariante: "pedido amplo de estoque por preco baixo deve rodar stock_search e responder com vehicle_offer_list real, nunca terminal-safe".
- **`detectBroadStockQuery(ctx)`** (deteccao GERAL, sem if por frase): regex sobre `normalizeText(leadMessage)` — `barat|economic|em conta|acessiv|preco baixo|mais barat`. Cobre "modelos baratos", "carros mais baratos", "opções econômicas", "algo mais em conta", "acessível".
- **Pre-seed:** se `detectRequestedModels` vazio E `detectBroadStockQuery` -> `stock_search({ broad: true })`. A fonte (`stock-source.ts`) ja **ordena por preco crescente** e, sem modelo/tipo/precoMax, retorna todo o estoque elegivel (preco/ano fail-closed). `limitCheapest(res, 5)` corta nas **5 mais em conta**. Os fatos entram no turno -> o `vehicle_offer_list` ancora -> **sem terminal-safe**.
- **Sem teto inventado** (regra do dono): nao passa `precoMax` (o lead nao deu teto); so ordena asc + limita. Estoque vazio -> resposta honesta (o compose ja trata "sem resultados", como no caso do Gol).
- Reusa o mecanismo existente (F2.7.5 `vehicle_offer_list` + render); nao cria handler novo nem muda contrato de estado.

## Testes — `tests/run-f2-7-9-cheap-stock.ts` (NOVO, 23 OK) + `test:f279`
`detectBroadStockQuery` (5 frases de preco -> true; "quero um onix"/"Boa tarde"/"tem gol?"/"horário" -> false). `limitCheapest` (ordena asc, limita 5, exclui o mais caro). **e2e via `runTurn`:** "Quais modelos baratos" -> rodou `stock_search(broad)`, **NAO terminal-safe**, fatos = 5 mais baratos, resposta cita o mais barato (Uno) grounded sem grude; "Tem carros mais baratos?"/"Quero opções econômicas" idem; **estoque vazio -> honesto, sem terminal-safe**; **modelo nomeado ("quero um onix") -> stock_search(modelo), NAO broad** (o gate `seededModels.length===0` protege).

## Gates
`npm run test:all` **EXIT=0** (F2.7.9 23 + F2.7.8 47 + todas). `npx tsc --noEmit` limpo. Scan: helper PURO, sem I/O. Sem SQL, sem mudar contrato de estado/tabela. v2/bridge/webhook intactos.

## Risco / nota
- A deteccao e por PRECO baixo (barato/economico/...). Browse generico sem preco ("quais modelos voce tem?") NAO esta coberto aqui (fica p/ fase propria se aparecer) — escopo do Codex era preco baixo.
- O LLM ainda escolhe os vehicleKeys do `vehicle_offer_list`; com os fatos presentes (5 mais baratos) ele ancora. Se quiser garantia 100% deterministica da lista, vira um handler proprio depois.

**Parado para auditoria do Codex (F2.7.9 + F2.7.8 r3.2). NAO empurrado. Nao avancar p/ CRM/handoff.**
