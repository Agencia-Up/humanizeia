# Handoff — F2.7.11 (P0) Fallback técnico vazando + guard de continuidade

Data: 2026-06-30
Executor: Claude (auditor: Codex). Gates verdes. Sem CRM/handoff/follow-up. Sem SQL. v2/bridge/webhook intactos. **NAO empurrado — aguarda auditoria do Codex.**

## P0 (print): o texto de SISTEMA "Desculpe a lentidão temporária…" foi pro LEAD numa conversa normal
Depois de uma lista válida, e de novo no "Boa tarde", o agente mandou o fallback técnico. Bloqueador de produção.

## Tarefa 1 — Diagnóstico read-only (evidência das tabelas, tenant ecb26258)
Duas sessões na mesma conversa (`wa:8ed137…`):
- **19:28 (pré-F2.7.10):** poll-6 "Certo, quais os carros mais baratos que tem?" -> lista (CORRETO); poll-7 "mais populares" -> **terminal_safe**. 2 turnos DIFERENTES (já diagnosticado; F2.7.10 corrigiu "populares").
- **20:18 (NOVO, o foco do P0):** **poll-1-34e6fc55 "Boa tarde"** (turnNumber 8, conversa COM histórico) -> **terminal_safe**.
  - `v3_decisions`: `reason_code=terminal_safe`, `reason_summary` = `POL-GROUND-PRICE` deny *"vehicle_offer_list: veículo 'revendamais:8022153' não encontrado nos fatos do turno"*; `composed=null`; sem facts. -> **O LLM tentou OFERTAR uma lista (vehicle_offer_list) para "Boa tarde", com chave ungrounded (não rodou stock_search neste turno) -> grounding nega -> terminal-safe.**
  - `v3_effect_outbox`: 1 `send_message`, order 1, status succeeded, `payload.text` = **"Desculpe a lentidão temporária. Como posso te ajudar a escolher seu veículo hoje?"** (o texto técnico saiu mesmo).
  - `v3_conversation_state`: turnNumber 8, recentTurns=16 (POPULADO), `offers.last=null` + `focus=null` (**issue C** — callback delivered não chega), `slots.interesse="onix, renegade"` (known). ⚠️recentTurns já tinha 2 "Desculpe a lentidão" (o fallback técnico virou MEMÓRIA).
- **Respostas às 5 sub-perguntas:** (a) NÃO é mesmo turn_id com 2 send_message; (b) **SIM, turnos diferentes próximos** (a lista e o terminal-safe são turnos distintos; o "Boa tarde" terminal-safe é outro turno ainda); (c) não é retry de evento pendente antigo (evento done, 1 turno); (d) NÃO é "validou e depois dispatcher mandou fallback" — o engine emite UMA decisão/turno e o terminal-safe CANCELA o comercial. A causa real = o LLM ofertou ungrounded para uma saudação.

## Tarefa 2 (FIX P0) — fallback técnico NUNCA vai pro lead — `engine/continuity-fallback.ts` (NOVO)
`buildContextualSdrReply(state)` (PURO): o texto despachado num terminal-safe passa a ser um **fallback de SDR conduzido pelo ESTADO** — nunca "Desculpe a lentidão":
- oferta recente nos `recentTurns` (R$/lista, ignorando o fallback antigo) -> "Quer ver as fotos de algum desses, ou prefere que eu filtre por valor, câmbio ou ano?";
- senão `slots.interesse` conhecido -> "Sobre o que você comentou (onix, renegade) — quer ver as opções, fotos, ou agendar uma visita?";
- senão -> descoberta neutra ("me conta o que procura…").
- `decision-engine.ts`: os DOIS pontos de terminal-safe (validação esgotada + erro/timeout) trocam o texto hardcoded por `buildContextualSdrReply(ctx.state)`. **O `reason_code` segue `terminal_safe`/`error`/`timeout` para LOGS** (diagnóstico interno preservado), só o texto ao lead virou comercial. Usa `recentTurns` (accepted-safe), não depende de offers/focus (C-blocked).

## Tarefa 3 — guard de CONTINUIDADE (proativo) — mesmo módulo
`detectContinuityIntent({leadMessage,state,claimExtractor})` + `buildContinuityTurnOutput(state)`: saudação/ack/comentário curto (`bom dia/ok/certo/entendi/bonito/legal/…`, por CLASSE, não por frase) SEM sinal comercial novo (sem foto/preço/estoque/modelo/agenda) e em conversa **já iniciada** (`alreadyIntroduced`) -> resposta determinística que CONDUZ pelo contexto (`buildContextualSdrReply`), **sem reiniciar, sem reapresentar, sem ofertar ungrounded** (que virava terminal-safe). 1º contato -> segue pro LLM (saudação inicial). Chamado no `conversation-engine` (ordem: photo -> ranking -> economy -> **continuity** -> runTurn). Assim "Boa tarde"/"ok"/"Bonito ele" pós-lista não caem mais em terminal-safe.

## Tarefa 4 — anti-dupla-resposta ruim
Engine = 1 decisão/turno + terminal-safe cancela comercial (SAFE_RESPONSE). Coberto por teste: oferta grounded -> NÃO terminal-safe; ungrounded -> terminal-safe SÓ com o fallback (sem 2º send_message comercial).

## Testes — `tests/run-f2-7-11-continuity-fallback.ts` (NOVO, 41 OK) + `test:f2711`
detectContinuity (saudação/ack/comentário curto introduzido -> true; comercial/modelo/pergunta -> false; 1º contato -> false; longa -> false); `buildContextualSdrReply` (3 ramos, NUNCA "Desculpe"); buildContinuity (não terminal-safe, só send_message); **T2:** grounding-fail -> terminal_safe interno MAS texto contextual (não "Desculpe") + referencia interesse; **erro de infra** (runQuery throw) -> error interno, texto contextual; **T4:** grounded -> não terminal-safe; **e2e (fallback):** msg que falha grounding -> outbox despacha fallback SDR, NUNCA "Desculpe a lentidão".
⭐**e2e de ROTEAMENTO (exigência do Codex) — via `runConversationTurn` com estado SEMEADO** (turnNumber>1 + recentTurns com oferta "Separei algumas: 1. Fiat Uno 2014 — R$ 29.990…"): (1) "Boa tarde" -> status committed, `decision.reasonCode==="continuity_conduct"`, exatamente 1 send_message / 0 send_media, payload.text sem "Desculpe", conduz pelo contexto (fotos/filtro/valor/câmbio/ano); (2) "Bonito ele" pós-oferta -> continuity_conduct, sem send_media, sem texto técnico, sem reapresentar; (3) NEGATIVO "tem onix?" -> NÃO continuity_conduct (segue comercial). Seed do StateStore in-memory via o Map `states` (`(p as any).states.set`).

## Gates
`npm run test:all` **EXIT=0** (F2.7.11 30 + todas). `tsc` limpo. Sem SQL, sem mudar contrato de estado/tabela. v2/bridge/webhook intactos.

## Risco / nota
- A continuidade é conservadora (≤6 palavras, por classe, exige `alreadyIntroduced`). Caso escape, o **fallback contextual (Task 2) é a rede de segurança** — o lead nunca vê texto de sistema.
- recentTurns ainda guarda os "Desculpe a lentidão" das falhas antigas desta conversa; em conversa NOVA não aparece. (Reset limpa.)

**Parado para auditoria do Codex (F2.7.11). NAO empurrado. Nao avancar p/ CRM/handoff.**
