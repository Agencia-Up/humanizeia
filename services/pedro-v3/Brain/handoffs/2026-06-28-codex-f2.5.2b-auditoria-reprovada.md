# Handoff - Auditoria Codex F2.5.2B

Data: 2026-06-28
Autor: Codex
Resultado: **reprovada; F2.5.2C bloqueada**

## Gates reexecutados

- `cmd /c npm.cmd run test:all`: 289 OK, 0 falhas.
- `cmd /c npm.cmd exec -- tsc --noEmit`: limpo.
- Nenhum efeito externo, CRM write, WhatsApp ou deploy encontrado.

Os gates verdes nao cobrem todos os invariantes prometidos. A suite B adicionou 21 checks, mas nao exercita redirect, timeout, limite de bytes, Authorization em redirects, cross-tenant da fonte, TTL/LRU/single-flight ou cache versionado. O teste integrado ainda resolve DNS real.

## Bloqueios obrigatorios da B.1

1. Remover o classificador por listas de modelos. Sem carroceria factual validada, usar `unknown`.
2. Gerar `vehicleKey = source:externalVehicleId|fingerprint`; nunca usar tenantId como source.
3. Entregar evidencia sanitizada da inspecao real dos feeds antes de fixar nomes de campos.
4. Tornar o HTTP provider-scoped, bloquear redirect cross-origin com headers sensiveis, validar GET JSON e usar DNS/transport injetaveis nos testes.
5. Implementar `TenantCatalog` dinamico, sem hardcode.
6. Unificar carga e normalizacao de estoque/fotos; hoje URL RevendaMais pura funciona no stock-source e falha no photo-source.
7. Endurecer decoder de `unknown`, validar URLs de fotos e bloquear URL insegura.
8. Versionar o cache por integracao/config e provar isolamento, TTL, LRU, single-flight e invalidacao.
9. Nao devolver duas ofertas com o mesmo vehicleKey em colisao de fingerprint.
10. Executor nao pode marcar a propria entrega como aprovada em auditoria.

## Restricoes

- Pedro v2 somente leitura.
- Sem banco, SQL, deploy, WhatsApp, CRM write, handoff, LLM real ou fase C/D.
- Preservar os 289 testes e adicionar cobertura determinista para os bloqueios.
- Parar novamente para auditoria do Codex.