# Handoff - F2.6K - grandfather BYOK (chave da plataforma por caminho backend seguro)

Data: 2026-06-29
Executor: Claude (auditor: Codex)
Escopo: Pedro v3 `Agent/` (resolver + gateway + testes) + SQL manual. **Sem deploy, sem banco, sem ativacao.**
`PEDRO_V3_PILOT_MODE` continua OFF.

## Bloqueador (dono/Codex)

A conta piloto do Douglas (`ecb26258-...`) NAO tem chave OpenAI propria no Vault — usa a chave da
PLATAFORMA, igual Bruno/Wander (comportamento correto e atual do v2). A F2.6J removeu a `OPENAI_API_KEY`
global (correto) mas deixou o v3 SO com `get_client_ai_key` -> quebraria contas grandfathered sem chave propria.

## Correcao: mesmo 3-tier do v2 (`_shared/aiKeys.ts`)

1. cliente COM chave propria -> usa a do cliente (`get_client_ai_key`);
2. conta GRANDFATHERED sem chave propria -> chave da PLATAFORMA;
3. conta NOVA sem chave propria -> fail-closed.

Regra factual REUTILIZADA do v2: `BYOK_GRANDFATHER_CUTOFF = 2026-06-16T03:00:00Z` (mesma constante).
Grandfather decidido lendo `profiles.created_at` com **fail-open** (erro/ausencia -> grandfathered),
identico ao `isAccountGrandfathered` do v2 — nunca derruba conta atual por falha de leitura.

## Caminho backend SEGURO da chave da plataforma (sem env no EasyPanel)

A chave da plataforma hoje so existe no env `OPENAI_API_KEY` do webhook v2 (o DB nao le isso). Para o v3
obter sem expor segredo ao container, criei a RPC service-role **`get_platform_ai_key(p_provider)`** que le
do **Vault** (`vault.decrypted_secrets`, mesmo mecanismo do `service_role_key` dos crons). Entregue como SQL
MANUAL (`Brain/sql/v3_f2_6k_platform_ai_key.sql`), nao executado.

**Menor ajuste operacional do dono** (uma vez, sem expor ao EasyPanel):
1. Rodar o patch SQL `v3_f2_6k_platform_ai_key.sql` (cria a RPC + privilegios service-role).
2. Cadastrar a chave da plataforma no Vault (a MESMA do `OPENAI_API_KEY` do v2):
   `select vault.create_secret('<CHAVE>', 'platform_openai_api_key');` (so no SQL Editor; nao commitar).
3. Verificar: `select name from vault.decrypted_secrets where name='platform_openai_api_key';`

Sem esse secret, contas grandfathered caem em fail-closed (turno degrada pro v2; sem chave global, sem dupla).

## O que foi feito

- `Agent/src/adapters/read/tenant-openai-key.ts` — resolver 3-tier (client -> grandfather/platform ->
  fail-closed) + `isGrandfathered` (fail-open) + `BYOK_GRANDFATHER_CUTOFF`. Client key e best-effort
  (erro cai pro fallback, igual v2); erro da PLATAFORMA -> `OPENAI_KEY_LOOKUP_FAILED` sanitizado.
- `Agent/src/runtime/supabase-service-gateway.ts` — allowlist: RPC `get_platform_ai_key` + tabela
  `profiles` (so leitura de `created_at` p/ grandfather).
- `Agent/tests/run-tenant-openai-key.ts` — reescrito p/ o 3-tier: **26 testes** adversariais.
- `Brain/sql/v3_f2_6k_platform_ai_key.sql` [NEW] — RPC + instrucao de Vault (manual).
- `services/pedro-v3/README.md` — nota BYOK com grandfather/platform + prereq SQL/Vault.
- `server.ts` NAO mudou (a assinatura do resolver e a mesma; segue passando o `gateway`).

## Testes (26 OK | 0 FALHA) — cobrem a regra 7

cutoff igual v2 · servico sem `OPENAI_API_KEY` · client key usa client (e nao consulta plataforma) ·
grandfathered sem client -> platform · conta nova sem client -> fail-closed (nem consulta plataforma) ·
**erro de leitura de profile -> fail-open -> platform** · profile inexistente -> fail-open · grandfathered
sem chave de plataforma -> fail-closed · erro da plataforma -> `OPENAI_KEY_LOOKUP_FAILED` sanitizado (sem
vazar) · erro da client key cai pro fallback · cross-tenant (B) nunca pega client key de A (platform ou
fail-closed) · boundary do cutoff (== grandfathered, +1ms novo) · chave com espaco tratada como ausente ·
tenantId vazio nao consulta gateway · JSON.stringify nao vaza (client e platform) · chave so via materialize.

## Gates (reais)

- `cd services/pedro-v3/Agent && npm run test:all` -> **EXIT=0** (+ `TENANT OPENAI KEY: 26 OK`).
- `npx tsc --noEmit` -> limpo.
- Regressao offline Pedro v2 -> **417 OK | 0 FALHA**.
- Bundle webhook v2: **N/A** (nao toquei webhook/bridge).
- Scan: `OPENAI_API_KEY` so como NOME de codigo de erro no adapter; nenhum log de segredo nas fontes.

## Riscos / pre-requisitos (pre-ativacao)

- **R-1**: rodar o SQL `v3_f2_6k_platform_ai_key.sql` + cadastrar `platform_openai_api_key` no Vault.
  Sem isso, a conta grandfathered (piloto) cai em fail-closed (degradacao segura pro v2).
- **R-2**: confirmar que `get_platform_ai_key` (security definer) consegue ler `vault.decrypted_secrets`
  (mesmo padrao dos crons; depende do owner/privilegio da function — validar na verificacao read-only).
- **R-3**: `profiles` entrou no table-allowlist do gateway service-role (so `created_at` e lido). Auditar.
- (Mantidos) `instance_id` do Aloan, `messages_update`, rotacao da service_role.

## O que NAO foi feito (por instrucao)

- Nenhuma chave no EasyPanel. Nenhum fallback inseguro. Nenhum deploy/`db push`/ativacao executados.
- Nao colei chave em container. Nao recuperei o valor de nenhuma chave. `PEDRO_V3_PILOT_MODE` OFF.

**Parado para auditoria do Codex (F2.6K).**
