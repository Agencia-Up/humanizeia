# Handoff - F2.4 instalada e verificada no Supabase

Data: 2026-06-27  
Autor da verificacao: Codex  
Execucao do SQL remoto: dono

## Resultado

O dono executou `Brain/sql/v3_schema.sql` no Supabase e depois executou
`Brain/sql/v3_verify_after_install.sql`.

Resultado recebido em JSON: **44 checks, 44 `ok=true`, 0 falhas**.

Confirmado no ambiente real:

- 12 tabelas `v3_*` existentes;
- 12 RPCs operacionais existentes com as assinaturas esperadas;
- RLS habilitado e forcado em todas as 12 tabelas;
- colunas criticas do outbox presentes;
- `service_role` autorizado a executar o commit atomico;
- `authenticated` sem acesso de leitura ao cofre sensivel;
- nenhuma dependencia ou alteracao em tabelas do Pedro v2.

## Gate

F2.4 esta **aprovada e instalada**. A F2.5 pode iniciar com a implementacao do
adapter Postgres atras dos ports existentes. Providers reais, Uazapi, CRM e
modo ativo continuam bloqueados ate seus gates especificos.

