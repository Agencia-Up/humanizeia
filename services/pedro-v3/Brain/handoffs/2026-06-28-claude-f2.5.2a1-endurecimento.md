# Handoff — 2026-06-28 — Claude → Codex (F2.5.2A.1: endurecimento contratual)

## 1. Escopo

A F2.5.2A passou (242/242), mas a auditoria do Codex achou lacunas contratuais. Implementei **só a A.1**
(endurecimento) nos arquivos da fatia. **Não iniciei B/C/D.** Sem HTTP/CRM/QueryRunner/LLM/rede; Kernel/v2/banco intocados.

## 2. As 9 correções (todas aplicadas)

1. **2 camadas de propriedade** — após `getOwnedAgent`, revalido `agent.id===ref.agentId` e `agent.tenantId===ref.tenantId`; idem funil (`fc.agentId/tenantId`); `StockIntegrationMetadataRow` ganhou `tenantId` e cada integração é validada vs `ref.tenantId`. Divergência → **`SOURCE_OWNERSHIP_MISMATCH`** (nunca aproveita os dados).
2. **`READ_SOURCE_FAILURE`** — `tryGateway` envolve CADA chamada do gateway; exceção → ConfigResult fail-closed; **nunca** lê/propaga `error.message`. Teste: gateway lança `Error("token=SECRET-CANARY prompt=PROMPT-CANARY")` → resultado `READ_SOURCE_FAILURE` e nenhum canário no ConfigResult.
3. **Imutabilidade real** — `qualificationQuestions`/`blockedCategories` clonados (`[...]`) + `Object.freeze`; `Object.freeze` no objeto final; `SecretRef` já frozen. Testes: `Object.isFrozen` no objeto e arrays; mutar arrays do seed após o load não altera a config.
4. **versionStamp composto** — `agent.updatedAt | funnel.updatedAt(quando funil) | provider:integrationId:integration.updatedAt`. Determinístico, **sem prompt/segredo**. Testes: muda com funil e com provider/integração.
5. **SecretRef tipado** — `provider: "revendamais"|"bndv"` (união fechada). `makeSecretRef` valida contra `SECRET_PROVIDER_ALLOWLIST`/`SECRET_PURPOSE_ALLOWLIST` (allowlist REAL de valores) + ids não-vazios; rejeita via `SecretRefError` que **nomeia o campo, não o valor**.
6. **CredentialProvider fail-closed** — `resolve(ref): ResolveSecretResult` discriminado; fake retorna `SECRET_NOT_FOUND`/`SECRET_OWNERSHIP_MISMATCH`/`SECRET_PROVIDER_MISMATCH` em vez de material "default". `TenantConfigSource` continua **sem chamar `resolve`**.
7. **Validação de metadata** — rejeita id vazio, tenant divergente, provider desconhecido, duplicado, timestamp inválido (`Date.parse`); **sem normalização silenciosa**.
8. **Testes adversariais novos** — todos os itens da lista do Codex (gateway mentiroso ×3, erro com segredo, frozen+mutação, versionStamp ×2, makeSecretRef ×4, resolve fail-closed ×3, metadata ×4).
9. **`Brain/08` consolidado** — reescrito como **especificação ÚNICA autoritativa**; trechos obsoletos **removidos** (não só marcados): `marca|modelo|ano`/índice, KnowledgeSource, seleção de CPF, `last_sync_at` como base, cópia de listas do v2.

## 3. Arquivos tocados (só da fatia)
`Agent/src/domain/{read-ports,credential-provider}.ts` · `Agent/src/adapters/read/{v2-read-gateway,tenant-config-source}.ts` · `Agent/src/adapters/read/fakes/{fake-v2-read-gateway,fake-credential-provider}.ts` · `Agent/tests/run-read-side.ts` · `Brain/{08,01,este handoff}`. **Kernel, v2, banco, package.json(scripts) intocados além do já existente.**

## 4. Gates
- `cmd /c npm.cmd run test:all` → **268 OK | 0 FALHA** (214 existentes + **54** read-side; eram 28).
- `npm.cmd exec -- tsc --noEmit` → limpo (exit 0).
- `rg` nas fontes da fatia: **sem** fetch/http, Uazapi, EffectDispatcher, escrita CRM, import do v2, `@supabase`/postgres. Strings `https://…`/`token` **só no teste** (canários; sem `fetch(`).

## 5. Próximo passo
1. **Codex audita a F2.5.2A.1.**
2. Aprovado → **F2.5.2B** (estoque + identidade §3.1 + categoria §3.4 + catálogo + fotos §3.2 + adapter HTTP §3.5), com a 1ª tarefa gated = confirmar o id externo real (RevendaMais/BNDV) por inspeção read-only controlada.

---
**Parado para auditoria do Codex (F2.5.2A.1).**
