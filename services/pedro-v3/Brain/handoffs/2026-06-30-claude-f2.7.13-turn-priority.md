# Handoff — F2.7.13 (P0) Prioridade do TURNO ATUAL vs memória antiga (jeep -> Argo)

Data: 2026-06-30
Executor: Claude (auditor: Codex). Gates verdes. Sem CRM/handoff/follow-up. Sem SQL. v2/bridge/webhook intactos. **NAO empurrado — aguarda auditoria do Codex.**

## P0 (print)
Lead: "Boa noite" / "voces tem jeep?" -> agente respondeu sobre **ARGO** ("não temos o modelo Argo"). Depois "quero Jeep" -> fallback genérico. O lead falou JEEP; o agente usou ARGO da memória.

## Tarefa 1 — Diagnóstico (banco, tenant ecb26258)
- **poll-2 "voces tem jeep?"** -> `reason_code=no_argo_in_stock`, summary *"Cliente perguntou pelo Argo"*. **O LLM achou que o lead pediu ARGO.**
- **poll-3 "quero Jeep"** -> `terminal_safe` -> fallback contextual.
- **`slots.interesse = "onix, renegade, argo, hb 20, 3, jeep"`** — memória POLUÍDA (a captura F2.7.7 acumula todo token, incl. "3" de "foto do 3" e "hb 20"). "Argo" veio daí.
- `lastRenderedOfferContext=null`; **"jeep" NÃO foi buscado** (é marca, não modelo do catálogo -> o pre-seed F2.7.4-D não rodou).
- **Causas:** (1) o turno atual ("jeep") NÃO tem prioridade sobre a memória antiga; (2) "jeep" (marca) não é detectado/buscado; (3) interesse acumula lixo.

## Tarefa 2/3/4 — Invariante de PRIORIDADE DO TURNO ATUAL
- **`engine/explicit-search.ts` (NOVO):** `computeTurnFrame({leadMessage,claimExtractor})` -> `{explicitModels, explicitBrands, explicitTypes, budgetMax, isNewCommercialIntent, isReferenceOnly}`. Marca por CLASSE (lista BR: jeep/fiat/chevrolet/...); tipo (suv/hatch/sedan/pickup); faixa ("ate N mil"). Usa o **TEXTO DO LEAD** (não a interpretação, que mapeava "jeep"->"argo").
- **`resolveExplicitSearchIntent`** (handler DETERMINISTICO): se ha intenção comercial nova (alvo ÚNICO), roda `stock_search` do que o lead pediu AGORA — marca via filtro `modelo` (o stock-source casa marca+modelo+versão, prod) — e devolve `offer` (5 mais em conta, ancorado) OU `none`. **MULTI-MODELO** ("onix ou argo", lead+interpretação, sem marca explícita) -> volta pro LLM (responde o bloco, F2.7.7). **`buildExplicitSearchTurnOutput`:** offer -> `vehicle_offer_list` + `renderedOfferContext` (reasonCode `explicit_offer`); none -> honesto "No momento não tenho {X}…" (reasonCode `explicit_not_found`, NÃO terminal-safe, NUNCA Argo). **Bypassa o LLM -> não usa memória antiga.**
- **`conversation-engine`:** ordem photo -> ranking -> economy -> **explicit-search** -> continuity -> runTurn. `continuity` agora NÃO dispara com intenção comercial nova (`turnHasNewCommercialIntent`).
- **`lastRenderedOfferContext`** segue lido SÓ pelo resolvedor de foto (ordinal/referência) — nunca p/ nova busca.

## Testes — `tests/run-f2-7-13-turn-priority.ts` (NOVO, 24 OK) + `test:f2713`
TurnFrame (jeep=marca, gol=modelo, sedan=tipo, "ate 60 mil"=faixa, "esse tem foto?"=referência, "Boa noite"=nada); explicit (jeep em estoque->offer Jeep; sem estoque->none; sedan->offer; "Boa noite"->null); build (offer não terminal-safe + renderedOfferContext; none honesto sem "Desculpe"); **e2e (estado SEMEADO com interesse='argo...' + lista Argo/Onix):** "voces tem jeep?" -> `explicit_offer` Jeep, **NUNCA Argo/Onix**, rodou stock_search jeep; "quero Jeep" -> ignora lista antiga, busca Jeep; "tem Gol?" -> Gol, nunca Onix; Jeep ausente -> `explicit_not_found` honesto, sem Argo/terminal-safe; continuity NÃO dispara em "quero Jeep"/"tem gol?".
⚠️**Testes existentes ajustados** (o explicit-search mudou o roteamento de inputs comerciais que esses e2e usavam p/ outras features): `run-phase2.ts` (shadow: input "ate 65 mil" -> "me ajuda a achar um carro?" p/ exercitar o caminho do LLM/harness); `run-f2-7-6-debounce.ts` (conteúdo "quero um onix" -> "quero ver opcoes", sem modelo, p/ a agregação ser observada via o LLM). A agregação/harness em si seguem intactos.

## Gates
`npm run test:all` **EXIT=0** (F2.7.13 24 + todas). `tsc` limpo. Sem SQL. v2/bridge/webhook intactos.

## ⚠️ Pendências / follow-ups (p/ o Codex)
1. **`deriveModelContext` deprioritizar interesse no turno novo (ponto 3 do dono):** o explicit-search já cobre o ALVO ÚNICO (bypassa o LLM/memória). No caminho MULTI-MODELO (LLM), o pre-seed busca os modelos do bloco, mas o `slots.interesse` antigo ainda pode ser surfaceado — defesa adicional pendente.
2. **Poluição do `slots.interesse`** ("…, 3, jeep"): a captura F2.7.7 acumula todo token (incl. "3"). É bug do extrator de slots (F2.7.7) — mitigado aqui porque o explicit-search bypassa a memória, mas a poluição em si fica p/ follow-up.

**Parado para auditoria do Codex (F2.7.13). NAO empurrado. Nao avancar p/ CRM/handoff.**
