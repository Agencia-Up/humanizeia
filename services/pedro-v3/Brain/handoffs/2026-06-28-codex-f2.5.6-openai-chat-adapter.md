# Handoff - F2.5.6 - OpenAI Chat Completions adapter

Data: 2026-06-28
Executor/auditor: Codex
Escopo: Agent + Brain. Sem chamada remota real, sem provider ativo, sem WhatsApp/CRM-write/handoff/agenda.

## O que foi implementado

1. `Agent/src/adapters/llm/openai-chat-model.ts`
   - `OpenAiChatCompletionsModel implements StructuredConversationModel`.
   - Protocolo especifico OpenAI Chat Completions: `https://api.openai.com/v1/chat/completions`.
   - Modelo default do piloto: `gpt-4.1-mini`.
   - Normaliza `openai/gpt-4.1-mini` para `gpt-4.1-mini`.
   - Rejeita modelo de outro provider (`anthropic/...`, `deepseek/...`) em vez de tentar fallback.
   - Envia `response_format: { type: "json_object" }`.
   - Usa mensagens `system` + `user`, com prompt do portal no `system` e payload estruturado no `user`.
   - `apiKey` fica em campo privado `#apiKey`; `toJSON()` nao vaza segredo.
   - Endpoint exige HTTPS, host allowlist e path `/v1/chat/completions`; rejeita query/hash/credencial embutida.
   - Timeout por `AbortController` + `Promise.race`, mesmo se o transporte nao cooperar.
   - Valida status HTTP, content-type JSON, limite de bytes, JSON valido e shape `choices[0].message.content`.
   - Suporta `message.content` string e array textual.

2. `Agent/tests/run-openai-adapter.ts`
   - Suite offline/fake-first da F2.5.6.
   - Nenhuma chamada de rede real e nenhuma leitura de `OPENAI_API_KEY`.
   - Transporte fake grava requests e devolve respostas controladas.

3. `Agent/package.json`
   - Novo script `test:openai`.
   - `test:all` agora inclui a suite OpenAI.

## Invariantes provados

- O OpenAI adapter nao decide negocio: ele so traduz interpret/propose/compose para Chat Completions.
- O `PromptBoundConversationAdapter` continua sendo o decoder autoritativo.
- `gpt-4.1-mini` e o default do piloto; `openai/...` e normalizado sem aceitar provider cruzado.
- Sem fallback automatico para Anthropic/DeepSeek.
- Segredo nao entra no body, nem em `JSON.stringify`.
- Erro 500 do provider vira erro tipado sanitizado; nao vaza body do provider.
- Resposta textual solta ou shape estranho falha fechado.
- Timeout aborta transporte travado.

## Gates executados

- `cmd /c npm.cmd run test:openai` -> **32 OK | 0 FALHA**.
- `cmd /c npm.cmd run test:all` -> **472 OK | 0 FALHA**.
- `cmd /c npx.cmd tsc --noEmit` -> limpo.
- Auditoria estatica nas fontes novas:
  - Sem `fetch(` real.
  - Sem `service_role`, `api_key_encrypted`, Uazapi, `EffectDispatcher`, `crm_write`, `handoff`, `schedule_visit`, `msg.includes` ou `rawMessage.includes`.
  - Achados de `OPENAI_API_KEY_MISSING` sao apenas codigo de erro, nao leitura de ambiente.

## Limites honestos

- Ainda nao houve chamada remota para OpenAI.
- Ainda nao existe leitura segura da chave real `OPENAI_API_KEY` no runtime v3.
- Ainda nao existe canary remoto.
- Ainda falta plugar este adapter no root/runtime real com credencial nova e EffectGate OFF.
- A rotacao/revogacao da `service_role` exposta continua obrigatoria antes de qualquer canary remoto.

## Proximo passo recomendado

F2.5.7: wiring controlado do adapter OpenAI no `CanaryShadowRoot`/bootstrap real, usando chave OpenAI injetada por configuracao segura, ainda em modo shadow e com transporte real somente apos rotacao da `service_role`. O teste remoto deve primeiro rodar sem WhatsApp, sem CRM-write, sem handoff e sem agenda.
