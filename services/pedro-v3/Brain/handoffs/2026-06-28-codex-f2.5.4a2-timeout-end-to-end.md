# Handoff - F2.5.4A.2 - Timeout end-to-end do PostgREST

Data: 2026-06-28
Executor/auditor: Codex

## Resultado

F2.5.4A.2 concluida e aprovada localmente. O deadline do wrapper Supabase read-only agora cobre a requisicao inteira: abertura, headers e consumo completo do corpo. Nenhuma rede real, credencial real, escrita remota ou efeito externo foi usado.

## Correcao

- `Agent/src/adapters/read/supabase-read-database.ts`
  - o timer nao e mais encerrado ao receber apenas os headers;
  - `readBounded` recebe o mesmo `AbortSignal` do request;
  - cada `reader.read()` compete com o abort do deadline;
  - timeout cancela o reader em best-effort e retorna somente `SUPABASE_READ_FAILURE`;
  - a segunda tentativa de cancelamento nao e aguardada, evitando que um `cancel()` defeituoso trave a protecao;
  - o timer e limpo no `finally` depois de todo o processamento.

- `Agent/tests/run-canary-wiring.ts`
  - novo transporte adversarial devolve headers imediatamente e um corpo que nunca produz bytes;
  - o proprio `cancel()` do stream retorna uma Promise que nunca resolve;
  - o teste prova termino dentro do watchdog, erro sanitizado e cancelamento do reader.

## Validacao

- `npm run test:canary` -> 54 OK | 0 FALHA.
- `npm run test:all` -> 399 OK | 0 FALHA.
- `npx tsc --noEmit` -> limpo.
- Gate estatico: sem JWT hardcoded, sem provider real, sem EffectDispatcher, sem WhatsApp/CRM-write/handoff e sem alteracao no Pedro v2.

## Status

F2.5.4A/A.1/A.2 APROVADAS LOCALMENTE. O wrapper read-only, o vinculo tenant/agente e o canary shadow offline estao liberados para a proxima fatia.

Canary remoto continua bloqueado ate a rotacao/revogacao da service_role exposta. Prompt -> LLM real e providers externos ainda nao estao ligados.

## Proximo passo sugerido

F2.5.4B offline: ligar a `TenantRuntimeConfig.promptText` a um adapter LLM tipado e completar a composicao de interpretacao, ClaimExtractor e TenantCatalog, mantendo EffectGate OFF e transportes externos fakes. Depois, rotacionar a credencial e executar o primeiro canary remoto somente read-only/shadow.