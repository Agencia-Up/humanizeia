# Handoff - F2.6L - observabilidade da falha de turno + diagnostico da nao-ativacao

Data: 2026-06-29
Executor: Claude (auditor: Codex)
Escopo: Pedro v3 `Agent/` + SQL manual. **Sem deploy, sem ativacao, sem rotacao.**

## Diagnostico do "v3 nao responde" (print do dono = v2)

Investigacao read-only (Supabase MCP) confirmou:
- **Agora o webhook NAO roteia pro v3.** Turno de teste real na instancia `aloan`: resposta SEM
  `routed:"pedro_v3"` + **0 eventos novos no `v3_inbox`** + o **v2** criou chat/turn/lead. Logo
  `pedroV3Pilot.enabled && mode==="active"` deu false -> `PEDRO_V3_PILOT_MODE` nao esta `active` (ou foi desligado).
- **Quando ESTEVE active (04:31), o v3 falhou**: 5 eventos no `v3_inbox` `pending`, `attempts=5`, `claimed_at`
  vazio, `last_error` VAZIO, `v3_outbox=0`. O turno quebra DEPOIS do ingest e ANTES de produzir saida — de
  forma consistente — e cai no v2.
- **Pre-requisitos OK** (nao sao o problema): `instance_id=6476a393` (setado), `get_platform_ai_key` existe,
  secret `platform_openai_api_key` no Vault, conta grandfathered (criada 2026-03-20).
- **Chave OpenAI DESCARTADA**: puxei a chave da plataforma do Vault (service-role) e testei contra a OpenAI —
  `GET /models = 200` (auth OK) e `chat gpt-4.1-mini = 200` (OK). A chave que o v3 usa e VALIDA. Coerente com
  o esperado: erro de provedor seria terminal-safe (geraria outbox), e o outbox esta zerado.

Conclusao: a falha e de RUNTIME no servico v3 (provavelmente lease/claim/commit ou config pos-ingest), e o
erro so existe no log do container (EasyPanel), invisivel pelo Supabase. Por isso a F2.6L.

## F2.6L — tornar a falha diagnosticavel pelo banco

- `Agent/src/runtime/sanitize-error.ts` [NEW] — `sanitizeTurnError(error)`: `name:code: msg` truncado em 300,
  REDIGINDO segredos (`sk-`, JWT `eyJ`, `Bearer`). Puro.
- `Agent/src/runtime/server.ts` — no catch do `runTurn` (turno), quando o evento JA foi ingerido, grava o
  motivo sanitizado em `v3_inbox.last_error` via RPC (best-effort; nunca mascara a falha original).
- `Agent/src/runtime/supabase-service-gateway.ts` — `v3_record_inbox_error` no allowlist de RPC.
- `Brain/sql/v3_f2_6l_inbox_error.sql` [NEW, MANUAL] — RPC `v3_record_inbox_error(p_tenant_id,p_event_id,p_error)`
  tenant-scoped, `security definer`, so `service_role`. Atualiza `v3_inbox.last_error`.
- `Agent/tests/run-sanitize-error.ts` [NEW] — 7 testes (tipado, redige sk-/JWT/Bearer, trunca 300, nao-Error/null).

## Gates (reais)

- `npm run test:all` -> **EXIT=0** (+ `SANITIZE ERROR: 7 OK`, `TENANT OPENAI KEY: 26 OK`).
- `npx tsc --noEmit` -> limpo.
- Offline Pedro v2 -> **418 OK | 0 FALHA**.
- Bundle webhook: N/A (nao toquei webhook/bridge).
- Scan: sem segredo/JWT/sk-/log de segredo nos arquivos novos (so o `grant ... service_role` legitimo no SQL).

## Passos do DONO para revelar a raiz (ordem)

1. Rodar `services/pedro-v3/Brain/sql/v3_f2_6l_inbox_error.sql` no SQL Editor (cria a RPC).
2. **Redeploy do servico Pedro v3 no EasyPanel** (pega o codigo F2.6L que grava o erro).
3. Setar `PEDRO_V3_PILOT_MODE=active` na edge function (so assim o webhook tenta o v3).
4. Avisar — eu disparo 1 turno de teste e LEIO `v3_inbox.last_error` via MCP -> raiz exata -> corrijo o runtime.

(Alternativa mais rapida: colar aqui o log do servico v3 no EasyPanel de um turno que falha — tem o erro completo.)

## O que NAO foi feito

- Nao ativei `PEDRO_V3_PILOT_MODE`. Nao deployei. Nao rodei SQL/`db push`. Nao rotacionei service_role.
- Nao mexi no webhook/bridge, CRM-write, handoff ou agenda.

**Parado para auditoria do Codex (F2.6L) + acoes do dono (1-3) para diagnostico final.**
