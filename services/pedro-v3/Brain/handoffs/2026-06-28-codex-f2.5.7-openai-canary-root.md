# Handoff - F2.5.7 - OpenAI canary root wiring

Data: 2026-06-28
Executor/auditor: Codex
Escopo: Agent + Brain. Sem chamada remota real, sem WhatsApp, sem CRM-write, sem handoff, sem agenda.

## O que foi implementado

1. `Agent/src/engine/canary-shadow-root.ts`
   - `CanaryReadDeps` agora aceita `model` direto **ou** `modelFactory(runtimeConfig)`.
   - O root carrega e valida o `TenantRuntimeConfig` primeiro; so depois materializa o modelo.
   - Sem `model` e sem `modelFactory`, falha fechado com `CanaryConfigError("MODEL_NOT_CONFIGURED")`.
   - Compatibilidade preservada: testes antigos continuam injetando `model` fake direto.

2. `Agent/src/engine/openai-canary-root.ts`
   - `OpenAiRuntimeSecret` encapsula a chave em campo privado e nao vaza em `JSON.stringify`.
   - `createOpenAiModelFactory` cria `OpenAiChatCompletionsModel` a partir do `TenantRuntimeConfig`.
   - Modelo vem do tenant/agente (`config.model`); se nulo, o adapter cai no default `gpt-4.1-mini`.
   - Modelo de outro provider (`anthropic/...`, `deepseek/...`) falha fechado pelo adapter OpenAI.
   - `createOpenAiCanaryShadowRoot` monta o `CanaryShadowRoot` com factory OpenAI, mantendo EffectGate/shadow.
   - `redactedOpenAiCanaryDepsSummary` fornece resumo operacional sem chave.

3. `Agent/tests/run-openai-canary-root.ts`
   - Nova suite F2.5.7 com Supabase fake + OpenAI transport fake.
   - Prova um turno shadow completo com interpret/propose/compose via OpenAI adapter.

4. `Agent/package.json`
   - Novo script `test:openai-root`.
   - `test:all` inclui a F2.5.7.

## Invariantes provados

- O modelo OpenAI e criado somente depois de carregar o agente/prompt/model do tenant.
- O prompt do portal chega ao `system` do OpenAI adapter.
- `openai/gpt-4.1-mini` vira `gpt-4.1-mini`.
- `model = null` no tenant cai no default `gpt-4.1-mini`.
- Modelo de outro provider bloqueia o create, sem fallback silencioso.
- Chave OpenAI so aparece no header do transporte fake; nao entra no body, no root, na config, no resultado ou em resumo operacional.
- O turno shadow commita, mas nao dispara provider externo real e nao aplica EffectOutcome.
- `CanaryShadowRoot` antigo continua funcionando com `model` fake direto.

## Gates executados

- `cmd /c npm.cmd run test:openai-root` -> **15 OK | 0 FALHA**.
- `cmd /c npm.cmd run test:all` -> **487 OK | 0 FALHA**.
- `cmd /c npx.cmd tsc --noEmit` -> limpo.
- `package.json` validado por `JSON.parse`.
- Auditoria estatica nas fontes novas/alteradas:
  - Sem `fetch(` real.
  - Sem `OPENAI_API_KEY`, `Deno.env`, `process.env`.
  - Sem `service_role`, `api_key_encrypted`, Uazapi, WhatsApp, `EffectDispatcher`, CRM-write, handoff, agenda, `msg.includes` ou `rawMessage.includes`.

## Limites honestos

- Ainda nao houve chamada remota real para OpenAI.
- Ainda nao existe canary remoto contra Supabase real.
- A chave OpenAI real ainda nao foi injetada em ambiente/runtime real.
- A `service_role` exposta continua pendente de rotacao/revogacao antes de qualquer canary remoto.
- EffectGate ativo, WhatsApp, CRM-write, handoff e agenda continuam bloqueados.

## Proximo passo recomendado

F2.5.8: preparar o canary remoto **shadow-only** com credenciais rotacionadas/seguras:

1. rotacionar/revogar a `service_role` exposta;
2. criar/injetar credencial nova somente no runtime de canary;
3. executar primeiro um probe remoto read-only do Supabase + OpenAI com EffectGate OFF;
4. registrar shadow comparison sem enviar WhatsApp, sem escrever CRM, sem handoff e sem agenda.

So depois disso discutir primeira conversa real no WhatsApp do agente de teste.
