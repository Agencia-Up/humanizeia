# Fase 7 — Unificação do contrato LLM + eliminação de chamadas repetidas + isolamento do legado

**Data:** 2026-07-18 (madrugada). **Autor:** Claude (executor). **Status:** FEITO no working tree, gates verdes, **NÃO commitado** — parado para auditoria do Codex + autorização do dono.

Baseline = `main f7194e7b` (RODADA 4 já em produção só no piloto). Missão do dono (ordem 1–7), decisão explícita: **Opção (a) — central_shadow também llmFirst=true**. Restrições do dono nesta rodada: *"Não precisa mexer na personalidade nem no prompt do portal; foco = memória operacional, tools, retry e ramo legado"*; *"Não priorizar o R1 de falha total institucional"*; *"manter brainMaxSteps como está; F7-2 = observabilidade only"*.

## O que foi feito (7 itens)

- **F7-1 — dedup/idempotência de tools no turno.** `runQueryDedup` generalizado em `central-engine.ts` (~2373): stock_search por `stockSearchFingerprint`; DEMAIS tools por `toolCallSignature` (`tool + JSON(input) ordenado`) num `toolExecCache`. **Mesma tool + mesmo input NUNCA executa 2×/turno.** (`tenant_business_info` continua deduplicado por `institutionalObs`/tópico.) Prova offline: `F2.66 [D]` (brain pede `vehicle_details` 2× → 1 execução). **Decisão:** cacheia TODOS os resultados (inclui falhas) — leitura mais ESTRITA do critério "nenhuma repetição de execução". O parêntese do dono "(tool que falhou pode ter 1 retry)" ficou DEFERIDO (hoje uma falha transitória fica cacheada; o lead recebe resposta honesta e re-pergunta no próximo turno). → decisão p/ Codex.
- **F7-2 — motivo de cada retry (anti retry-storm), sem mexer no teto.** (workflow) `classifyRetryReason(code,msg)` puro + `retryReasons`/`retryReasonCounts` em `decision_final` e `CentralTurnResult`. Derivado POST-LOOP das observações `!ok` (só LÊ, impossível alterar decisão/cap). `brainMaxSteps` intocado (default 4), como instruído.
- **F7-3 — catálogo compacto de modelos no contexto.** (workflow) Frame passou a expor `prepared.tenantCatalog` (já carregado, sem query nova) → a LLM corrige typo (danster→Duster) por contexto, sem regex comercial. `TurnFrame`/`turn-frame-builder`/`central-engine`/`openai-agent-brain` + F2.14.
- **F7-4 — schemas auxiliares em json_schema strict.** (workflow) Novo `response-draft-schema.ts` (fonte única do `ResponseDraft{parts}`). `#rewriteDraftForForm` + `#critique` + `compose` → json_schema STRICT. `interpret`/`propose` → json_schema strict:false DELIBERADO (os decoders em `prompt-bound-conversation.ts` rejeitam opcionais preenchidos como null; strict:true quebraria o decode). **Nenhum `response_format: json_object` nos 4 caminhos LLM ATIVOS.** Sobra 1 `json_object` em `eval/real-harness.ts:439` = **o JUIZ da suíte de eval** (ferramenta de teste, não runtime do agente) — deixado intocado.
- **F7-5 — central_shadow roda llmFirst=true.** `central-shadow-runner.ts` passa `llmFirst: true` (espelha `#processCentralActive`). Efeitos externos seguem OFF (store isolado, zero dispatch, `providerCapability` none). O shadow agora observa EXATAMENTE o agente de produção.
- **F7-6 — isolamento DEFINITIVO do ramo legado.** Novo módulo de POLÍTICA `src/engine/legacy/legacy-replay.ts` (`isLegacyReplayEnabled`, `LEGACY_DETERMINISTIC_SOURCES`, `assertReplayWiring`, `assertLegacyAuthoringAuthorized`). Novo arg `legacyCommercialReplay?: boolean` (default false). O ramo `else` comercial determinístico (foto/institucional/desengajamento/mais-opções/recuperação/recall) só roda sob `legacyReplayEnabled = !llmFirst && legacyCommercialReplay`. Fora disso → `buildBrainUnavailableResponse` (nota de outage). Trim + recall re-gateados em `legacyReplayEnabled`. **Duas invariantes fail-closed:** fiação (`llmFirst+replay` → throw) e saída (`deterministic_*` fora do replay → throw). Banner in-file marcando os builders como REPLAY-ONLY. **Decisão:** a RELOCAÇÃO FÍSICA dos 5 builders p/ o módulo `legacy/` ficou DEFERIDA (acoplamento profundo com `PolicyEngine`/`finalize`/`ensureSendMessage`/`canonicalVehicleLabel`/predicados de foto/`selectPhotos` → mover exigiria expor ~15 símbolos internos ou DI larga; risco alto p/ ganho cosmético). **O isolamento COMPORTAMENTAL já é definitivo e provado** (o ramo é inalcançável em produção por 3 mecanismos independentes). → Codex decide se quer a relocação física.
- **F7-7 — suíte + smoke + adversarial.** Abaixo.

## Gates (verdes, grátis)
- `tsc --noEmit` EXIT 0.
- `npm run test:all` EXIT 0 — ~90 suítes, incluindo **F2.66 (novo) 30 OK**, scan anti-fallback **20 OK** (reforçado de 15), F2.13 46, F2.15 19, F2.16 5, F2.17 14.
- Testes tocados/criados: **novo** `run-f2-66-legacy-isolation.ts` (30 OK: política pura + eixos Produção/Replay/Não-autorizado + fiação + SHADOW + dedup F7-1). `run-central-no-generic-fallback.ts` reancorado ao novo `else if (!legacyReplayEnabled)` + assert da invariante de saída + assert do gate. `F2.15`/`F2.17` opt-in explícito `legacyCommercialReplay: true` (são replay legado: singleAuthor SEM llmFirst).

## Smoke real gpt-4.1-mini (chave de plataforma, efeitos OFF)
`PEDRO_V3_AI_PROVIDER=openai SMOKE_ONLY=A|B|C npx tsx eval/run-fase-simplification-deepseek-smoke.ts` (tenant piloto ecb26258, model=gpt-4.1-mini, promptSha 04b5a62c). **TOTAL technical_fallback = 0 nos 3 cenários; 100% LLM-autorado.**
- **B (anúncio Compass) = PROVA MAIS LIMPA.** T1 aterra o Compass 2019 do anúncio (branco/automático/82.000 km/R$96.990, SEM lista ampla). T2 responde do contexto (0 tools, brainSteps=0). T3 envia foto do veículo CERTO (`send_media revendamais:7894915`). 
- **A (SUV até 100k):** lista correta (Duster/2008/Renegade), 0 fallback, LLM-autorado.
- **C (endereço→horário):** endereço + horário corretos, 0 fallback, não pede nome fora de hora, não inventa.

## ⭐ ACHADO #1 p/ Codex/dono — RETRY-STORM no nível de CHAMADA LLM (não resolvido nesta rodada)
O gpt-4.1-mini **re-pede a MESMA tool depois de resolvida**: C-T1 pediu `tenant_business_info` **6×** (7 chamadas LLP p/ "onde fica a loja?"); A-T1 re-pediu `stock_search` até estourar o teto de 8 chamadas do cenário (abortou o T2 do smoke — artefato do teto por-cenário do harness, não da produção: em prod cada turno tem seu brainMaxSteps).
- **F7-1 protege a EXECUÇÃO** (2 stock_search / 1 institucional apesar de N pedidos — o cofre/estoque NÃO é batido 6×; era esse o incidente do endereço).
- **F7-2 torna OBSERVÁVEL** (`retryReasons`=dup_tool/…).
- **NÃO eliminado:** o modelo ignora o feedback "já retornou, escolha a forma" e re-pede. É comportamento pré-existente (RODADA 4 já era `brain_retry`). Colapsar os PEDIDOS exigiria feedback mais forte ("NÃO chame de novo; componha agora") ou baixar o teto — **fora do escopo desta rodada** ("manter brainMaxSteps; F7-2 = observabilidade only"). A mission title ("eliminação de chamadas repetidas") só é 100% atingida no nível de EXECUÇÃO; no nível de CHAMADA LLM segue como follow-up. **Recomendação:** próxima rodada — tornar o deny de dup-tool terminal p/ o passo (força finalização) OU teto adaptativo por domínio.

## Notas
- **"2 prints":** o conteúdo exato dos 2 prints originais não está no contexto desta sessão; o smoke cobriu os 3 alvos nomeados (Compass/SUV/endereço). O incidente do endereço (6×) = cenário C. Se o dono passar os 2 prints, replico como cenários D/E.
- **Adversarial (isolamento):** `legacyCommercialReplay` NÃO tem chamador de produção (só a def do engine + o módulo de política + F2.15/F2.17/F2.66). Ambos os chamadores de produção (`pilot-active-root:423`, `central-shadow-runner:119`) passam `llmFirst: true` e nunca o replay. Critério essencial do dono **PROVADO**: nenhum central_active/central_shadow produz `deterministic_*`.
- **NÃO commitado.** Deixado verde p/ revisão do Codex de manhã + autorização do dono.

## Arquivos tocados (só services/pedro-v3, working tree)
`src/engine/legacy/legacy-replay.ts` (NOVO), `src/engine/central-engine.ts` (arg+destructure+invariantes+gate+banner; + F7-1/F7-2/F7-3 do workflow), `src/engine/central-shadow-runner.ts` (llmFirst), `src/adapters/llm/response-draft-schema.ts` (NOVO, F7-4), `openai-agent-brain.ts`/`structured-json-model.ts`/`openai-chat-model.ts` (F7-3/F7-4), `src/domain/agent-brain.ts`+`turn-frame-builder.ts` (F7-3). Testes: `run-f2-66-legacy-isolation.ts` (NOVO), `run-central-no-generic-fallback.ts`, `run-f2-15`, `run-f2-17`, `run-f2-14`, `run-openai-adapter.ts`, `package.json` (test:all += f266).
